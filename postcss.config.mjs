// Empty PostCSS config — stops Vite's upward search before it finds the broken
// config at C:\Users\Arthu\postcss.config.mjs. This project doesn't use PostCSS
// (no Tailwind, autoprefixer, etc.), so an empty plugins array is correct.
export default { plugins: [] };
