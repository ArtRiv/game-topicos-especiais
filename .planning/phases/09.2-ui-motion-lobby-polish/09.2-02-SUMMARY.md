---
phase: 09.2
plan: 02
subsystem: ui
tags: [phaser3, cinematic, bitmaptext, music-sync, checkpoint, human-verify]
dependency-graph:
  requires:
    - SplashScene (from 09.2-01)
    - MainMenuScene cinematic (from 09.2-01)
    - MusicManager (from 09.2-01)
    - scene-transition helper (from 09.2-01)
  provides:
    - Verified Splash -> MainMenu -> Lobby cinematic flow (locked baseline for Plans 03-04)
  affects: []
tech-stack:
  added: []
  patterns:
    - Human-verify checkpoint as the only acceptance gate when a plan is pure verification (no source edits)
key-files:
  created:
    - .planning/phases/09.2-ui-motion-lobby-polish/09.2-02-SUMMARY.md
  modified: []
decisions:
  - Plan 01's cinematic implementation matched the UI-SPEC §Splash + §Main Menu Cinematic beat tables exactly — zero source drift, zero patches required.
  - User signed off on the cinematic via the Task 2 human-verify checkpoint ("approved" — 2026-05-21).
metrics:
  duration: "~15 min (audit + checkpoint approval, no implementation)"
  completed: 2026-05-21
---

# Phase 09.2 Plan 02: Splash + MainMenu Cinematic Verification Summary

Verification-only plan: confirmed Plan 01's Splash → MainMenu cinematic matches the UI-SPEC beat tables exactly via source audit, then locked in user sign-off through a human-verify browser checkpoint. No source patches needed.

## Tasks

| # | Name | Commit | Files |
| - | ---- | ------ | ----- |
| 1 | Self-audit ported cinematic against UI-SPEC beat table | (audit — no commit) | src/scenes/main-menu-scene.ts, src/scenes/splash-scene.ts (read-only) |
| 2 | Human verify Splash→MainMenu cinematic in browser | (checkpoint — user approved) | n/a |

## What Was Verified

### Task 1 — Source Audit (zero drift)

All UI-SPEC beats verified present in source via grep:

**`src/scenes/main-menu-scene.ts`:**
- `MUSIC_DROP_MS = 2_000` (line 21) — canonical constant name preserved
- `cinematicPlayed` module-level flag (line 35) gates the 2s hold on first visit
- `this.time.delayedCall(MUSIC_DROP_MS, ...)` (line 87) drives the t=2000ms title slam
- Title slam: `scaleX: 1` (line 94) + `ease: 'Back.Out'` (line 97) — overshoot-then-settle
- Subtitle + menu items: `ease: 'Quad.Out'` (lines 113, 126)
- Hover tint: `TINT_MENU_HOVER = 0xffc857` (line 30)
- Hover scale: `scaleX: 1.05, scaleY: 1.05, duration: 100, ease: 'Quad.Out'` (line 207)
- Unhover return: `scaleX: 1, scaleY: 1, duration: 100, ease: 'Quad.Out'` (line 211)
- Routing: `startScene(this, SCENE_KEYS.LOBBY_SCENE, 300)` for both CRIAR LOBBY and ENTRAR EM LOBBY (lines 184–185)

**`src/scenes/splash-scene.ts`:**
- Blink easing: `ease: 'Sine.InOut'` (line 52)
- Dismiss handler: `FADE_OUT_COMPLETE` (line 73) → `scene.start(MAIN_MENU_SCENE)`

**Forbidden patterns (zero matches):**
- `new Phaser.GameObjects.Text` — none
- `setResolution(` — none in active code (only one historical reference in a comment in splash-scene.ts line 38, explicitly documenting the BitmapText migration away from it)
- `this.add.text(` — none

### Task 2 — Human-Verify Checkpoint (approved)

User exercised the full 10-step verification protocol from the plan (Splash blink → click → fade → 2s hold → title slam synced to music → subtitle → staggered menu items → hover tint/scale → click routing to Lobby → back-nav skipping the cinematic via `cinematicPlayed`). Reply: **"approved"**.

## Deviations from Plan

None — plan executed exactly as written. Task 1 audit found zero drift between Plan 01's output and the UI-SPEC beat tables, so no source patches were required. Task 2 cleared on the first checkpoint without re-checkpointing.

## Authentication Gates

None.

## Outcome

The Splash → MainMenu → Lobby cinematic baseline is locked in. Plans 03 (Lobby start fade + music duck — already complete) and 04 (LoadingScene cinematic — already complete) referenced its MusicManager constants and scene-transition helper without contention. Only Plan 05 (map thumbnail downscale) remains in Phase 09.2.

## Self-Check: PASSED

- File exists: `src/scenes/main-menu-scene.ts` — FOUND
- File exists: `src/scenes/splash-scene.ts` — FOUND
- All required grep patterns matched (see Task 1 above)
- No forbidden patterns present
- No new commits required for this plan (Task 1 was audit-only, Task 2 was human verification)
