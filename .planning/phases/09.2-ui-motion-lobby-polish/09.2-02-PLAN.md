---
phase: 09.2
plan: 02
type: execute
wave: 2
depends_on: [09.2-01]
files_modified:
  - src/scenes/main-menu-scene.ts
  - src/scenes/splash-scene.ts
autonomous: false
requirements: []
tags: [phaser3, cinematic, bitmaptext, music-sync, checkpoint]
must_haves:
  truths:
    - "SplashScene blinks 'PRESS ANYTHING TO GET HIGH IN THE FANTASY' and dismisses on any input"
    - "MainMenuScene fades in, music plays, and at t=2000ms title slams in (scale 1.3->1.0 Back.Out + α 0->1)"
    - "Subtitle '- ONLINE EDITION -' fades in at t=2150ms; menu items stagger 60ms starting at t=2200ms"
    - "Hovering a menu item tints gold 0xFFC857 and scales 1.05; clicking CRIAR/ENTRAR routes to LobbyScene"
    - "Second visit to MainMenuScene (after stub-scene back-nav) skips the 2s hold (cinematicPlayed flag)"
  artifacts:
    - path: "src/scenes/main-menu-scene.ts"
      provides: "Cinematic intro + 6 menu items + hover tweens"
    - path: "src/scenes/splash-scene.ts"
      provides: "Blinking gate"
  key_links:
    - from: "src/scenes/splash-scene.ts"
      to: "MainMenuScene"
      via: "fadeOut(400ms) -> scene.start(MAIN_MENU_SCENE)"
      pattern: "scene\\.start\\(SCENE_KEYS\\.MAIN_MENU_SCENE"
    - from: "src/scenes/main-menu-scene.ts"
      to: "MUSIC_DROP_MS timing"
      via: "this.time.delayedCall(MUSIC_DROP_MS, ...)"
      pattern: "delayedCall\\(MUSIC_DROP_MS"
    - from: "MainMenuScene menu items"
      to: "LobbyScene"
      via: "startScene(this, SCENE_KEYS.LOBBY_SCENE, 300)"
      pattern: "startScene\\(this, SCENE_KEYS\\.LOBBY_SCENE"
---

<objective>
Verify the cinematic produced in Plan 01 matches UI-SPEC §"Main Menu Cinematic" and §"Splash Scene" exactly. This plan ships as a tiny tuning pass plus a human-verify checkpoint — Plan 01 already wrote the cinematic; this plan exercises it end-to-end with the dev server and locks in any drift (timing constants, hover tints, copy strings) before subsequent plans build on top.

Purpose: Splash→MainMenu→Lobby flow is the user's first impression of v1.2; bad polish here taints the rest of the phase.
Output: Verified cinematic + minor timing fixups (if needed); user signs off via checkpoint before Plan 04 (LOADING cinematic) builds on the same MusicManager.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-ui-motion-lobby-polish/09.2-CONTEXT.md
@.planning/phases/09.2-ui-motion-lobby-polish/09.2-UI-SPEC.md
@.planning/phases/09.2-ui-motion-lobby-polish/09.2-01-SUMMARY.md
@src/scenes/main-menu-scene.ts
@src/scenes/splash-scene.ts
@src/common/music-manager.ts
@skills/tweens/SKILL.md
@skills/cameras/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-audit ported cinematic against UI-SPEC beat table</name>
  <read_first>
    - src/scenes/main-menu-scene.ts (as written by Plan 01)
    - src/scenes/splash-scene.ts (as written by Plan 01)
    - .planning/phases/09.2-ui-motion-lobby-polish/09.2-UI-SPEC.md §"Motion &amp; Interaction Contract" (Splash + Main Menu Cinematic + Menu Item Hover/Click)
  </read_first>
  <action>
Walk through UI-SPEC §"Splash Scene" and §"Main Menu Cinematic" line-by-line, comparing each beat to what Plan 01 produced. For each row in the beat tables, confirm one of:
- Beat is implemented correctly → no change.
- Beat differs from UI-SPEC → patch the source to match.

Specific beats to verify exactly:

**Splash:**
- Background `0x000000` full canvas, α=1. ✓
- BitmapText copy `PRESS ANYTHING TO\nGET HIGH IN THE\nFANTASY` (UI-SPEC §Copywriting Contract row "Primary CTA"). Tint `0xFFFFFF`.
- Blink: α 0.3 ↔ 1.0 over 800ms per cycle, Sine.InOut. UI-SPEC says "800ms per cycle, looping". Plan 01 used `duration: 400` + `yoyo: true` → effective cycle = 800ms ✓.
- Dismiss: `cameras.main.fadeOut(400, 0, 0, 0)` → `FADE_OUT_COMPLETE` → `scene.start(MAIN_MENU_SCENE)`. ✓.

**MainMenu (first-visit cinematic):**
- t=0: `fadeIn(400, 0, 0, 0)` + `playMenu(this)` + all UI α=0.
- t=2000 (`MUSIC_DROP_MS`): title slam (scale 1.3→1.0 over 250ms Back.Out + α 0→1 over 150ms Linear).
- t=2150: subtitle α 0→1 + y offset `+8 → 0`, 200ms Quad.Out.
- t=2200 + (i × 60): menu items α 0→1 + x offset `-8 → 0`, 200ms Quad.Out.

**MainMenu (subsequent visits, cinematicPlayed===true):** title/subtitle/menu items render at final state immediately. ✓.

**Menu Item Hover/Click:**
- Idle: tint `0xFFFFFF`, no scale.
- Hover: tint `0xFFC857`, scale 1.0→1.05 over 100ms Quad.Out. ✓.
- Unhover: tint `0xFFFFFF`, scale 1.05→1.0 over 100ms Quad.Out.
- Click: `startScene(this, TARGET_KEY, 300)`.

If a beat doesn't match Plan 01's output, edit `src/scenes/main-menu-scene.ts` / `src/scenes/splash-scene.ts` to match. Most likely fixups:
- If `pointerout` doesn't tween scale back to 1.0 → add the inverse tween.
- If subtitle/menu-item y/x offsets weren't applied → fix the pre-tween initial position assignments.
- If `MUSIC_DROP_MS` constant got renamed → restore the exact name (referenced by tests/specs).
  </action>
  <verify>
    <automated>cd C:\Users\Arthu\Desktop\code\game-topicos-especiais; npm run build; if ($LASTEXITCODE -ne 0) { Write-Host "BUILD FAILED"; exit 1 }; $mm = "src/scenes/main-menu-scene.ts"; $sp = "src/scenes/splash-scene.ts"; $required = @(@{ file=$mm; pat="MUSIC_DROP_MS = 2_000|MUSIC_DROP_MS = 2000" },@{ file=$mm; pat="Back\.Out" },@{ file=$mm; pat="Quad\.Out" },@{ file=$mm; pat="scaleX: 1\.05|scaleX:1\.05" },@{ file=$mm; pat="scaleX: 1," },@{ file=$mm; pat="0xffc857|0xFFC857" },@{ file=$sp; pat="Sine\.InOut" },@{ file=$sp; pat="FADE_OUT_COMPLETE" }); $fail = $false; foreach ($r in $required) { if (-not (Select-String -Path $r.file -Pattern $r.pat -Quiet)) { Write-Host ("MISSING " + $r.pat + " in " + $r.file); $fail = $true } }; $forbidden = Select-String -Path $mm,$sp -Pattern "new Phaser\.GameObjects\.Text|\.setResolution\(|this\.add\.text\("; if ($forbidden) { Write-Host "FORBIDDEN PATTERN FOUND:"; $forbidden | ForEach-Object { Write-Host $_ }; $fail = $true }; if ($fail) { exit 1 }; Write-Host "VERIFY OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MUSIC_DROP_MS = 2_000\\|MUSIC_DROP_MS = 2000" src/scenes/main-menu-scene.ts` matches.
    - `grep "Back.Out" src/scenes/main-menu-scene.ts` matches at least once (title slam).
    - `grep "Quad.Out" src/scenes/main-menu-scene.ts` matches at least twice (subtitle + menu items).
    - `grep "scaleX: 1.05\\|scaleX:1.05" src/scenes/main-menu-scene.ts` matches (hover scale).
    - `grep "scaleX: 1," src/scenes/main-menu-scene.ts` matches (unhover scale return).
    - `grep "0xffc857\\|0xFFC857" src/scenes/main-menu-scene.ts` matches (hover tint).
    - `grep "ease: 'Sine.InOut'\\|ease:'Sine.InOut'" src/scenes/splash-scene.ts` matches (blink easing).
    - `grep "FADE_OUT_COMPLETE" src/scenes/splash-scene.ts` matches.
    - `grep -E "new Phaser\\.GameObjects\\.Text|\\.setResolution\\(|this\\.add\\.text\\(" src/scenes/main-menu-scene.ts src/scenes/splash-scene.ts` returns NOTHING.
    - TypeScript build passes with zero errors.
  </acceptance_criteria>
  <done>All UI-SPEC beats verified in source; build passes; no BitmapText regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Splash→MainMenu cinematic in browser</name>
  <what-built>
Splash gate + MainMenuScene cinematic + 6-item menu wiring to LobbyScene / stub scenes, all rendered via BitmapText, with menu music starting on Splash and the title slam at t=2000ms matching the menu_music.ogg drop.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open `http://localhost:5173/`.
2. **Splash:** Confirm pure-black background, blinking "PRESS ANYTHING TO / GET HIGH IN THE / FANTASY" centered, smooth α pulse (no choppy 0/1 toggle). Confirm menu music starts playing (you should hear menu_music.ogg start within ~1 frame of first user gesture, due to autoplay-unlock handling).
3. Click anywhere. Confirm 400ms fade-to-black, then MainMenuScene appears with `landscape.mp4` video background (covers the canvas, not pixel-art smeared — should look smooth thanks to LINEAR filter), vignette overlay darkening the background.
4. **First-visit cinematic:** For 2 seconds, you should see ONLY the video background + vignette (no title/subtitle/menu visible yet). At t≈2s — coinciding with the menu music drop — `HIGH FANTASY` title slams in (brief overshoot scale, then settles), subtitle `- ONLINE EDITION -` fades in just after with a small upward drift, and the 6 menu items fade in one-by-one from the left (60ms apart).
5. **Hover/click:** Hover each menu item — confirm gold tint (`#FFC857`) + subtle scale-up. Unhover → return to white + scale-down.
6. Click `CRIAR LOBBY` → confirm 300ms fade then LobbyScene (current multiplayer Connect view).
7. Press the browser back/refresh and return; click into MainMenu via Splash again — confirm cinematic plays AGAIN (because cinematicPlayed flag is per-page-load; module-level state resets on hard refresh).
8. From MainMenu click `CONTA` → confirm fade to AccountScene stub ("CONTA / EM BREVE / PRESS ESC TO RETURN"). Press ESC → confirm fade back to MainMenu. **This time the cinematic should NOT play** — title/subtitle/menu items appear immediately at final state (cinematicPlayed === true within the same session).
9. Try `OPCOES`, `CREDITOS`, `ENTRAR EM LOBBY`, `SAIR` (check browser console for `[MainMenu] SAIR pressed` log).
10. Confirm no `WARN`/`ERROR` messages in the browser console related to missing assets, BitmapText, or audio.
  </how-to-verify>
  <resume-signal>Reply "approved" if all 10 steps pass, otherwise list which step failed and the observed behavior. The planner/executor will patch and re-checkpoint.</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval, this plan is done. The cinematic is now locked-in and Plans 03–04 can reference its components (MusicManager state, scene-transition helper) with confidence.
</verification>

<success_criteria>
- All UI-SPEC §Splash + §Main Menu Cinematic beats verified in source via grep.
- TypeScript build is clean.
- Human checkpoint signed off — cinematic plays correctly, music syncs to title slam, hover/click works, BitmapText renders crisply, stub-scene navigation behaves per spec.
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-ui-motion-lobby-polish/09.2-02-SUMMARY.md`.
</output>
