---
phase: 09.2
plan: 05
subsystem: ui
tags: [assets, imagemagick, thumbnails, pixel-art, lobby, loading-scene]
dependency-graph:
  requires:
    - public/assets/images/levels/world/world_background.png (UI-branch port, Plan 09.2-01 Task 1)
    - public/assets/images/levels/dungeon_1/dungeon_1_background.png (UI-branch port, Plan 09.2-01 Task 1)
    - ASSET_KEYS.MAP_THUMB_WORLD / MAP_THUMB_DUNGEON_1 wiring (Phase 9.1-01)
    - PreloadScene + LobbyScene explicit this.load.image() calls (Phase 9.1-01)
    - MAP_POOL.thumbnailKey references (Phase 09)
  provides:
    - Final 96x64 pixel-art preview of Open Field map
    - Final 96x64 pixel-art preview of Dungeon map
    - Phase 9.2 ROADMAP success criterion #3 (placeholder W/D rectangles removed)
  affects:
    - LobbyScene map-browser row thumbnails (rendered from existing texture keys)
    - LoadingScene cinematic Beat 2 preview (Plan 09.2-04 references mapEntry.thumbnailKey)
tech-stack:
  added: []
  patterns:
    - ImageMagick v7 nearest-neighbor downscale (`-filter Point`) for pixel-art preservation
    - `-resize 96x64^ -gravity center -extent 96x64` fill-then-center-crop pipeline (CONTEXT.md D-15)
key-files:
  created:
    - public/assets/images/levels/world/thumbnail.png (96x64, 1244 B)
    - public/assets/images/levels/dungeon_1/thumbnail.png (96x64, 928 B)
  modified: []
decisions:
  - ImageMagick downscale executed outside the agent by the orchestrator with `magick <src> -filter Point -resize 96x64^ -gravity center -extent 96x64 <dst>`; both outputs verified `PNG 96x64` via `magick identify`.
  - Zero code changes -- all wiring (ASSET_KEYS, PreloadScene/LobbyScene loads, MAP_POOL.thumbnailKey, LoadingScene cinematic) was already in place from Phases 09, 9.1-01, and 09.2-04. This plan was a pure asset swap.
  - Task 2 human-verify checkpoint auto-approved by user; they will catch any rendering issue in their next play session and re-open Plan 05 with a follow-up if needed.
metrics:
  duration: "~5 min (orchestrator pre-ran ImageMagick; agent finalized)"
  completed: 2026-05-21
---

# Phase 09.2 Plan 05: Map Thumbnail Pixel-Art Downscale Summary

Replace placeholder green-"W" / purple-"D" thumbnail PNGs with 96x64 nearest-neighbor downscales of the real `world_background.png` and `dungeon_1_background.png` map art. Pure asset swap -- existing `ASSET_KEYS.MAP_THUMB_*` wiring from Phase 9.1-01 picks them up automatically. Closes Phase 9.2 ROADMAP success criterion #3 and ships Phase 9.2.

## Tasks

| # | Name | Commit | Files |
| - | ---- | ------ | ----- |
| 1 | ImageMagick 96x64 nearest-neighbor downscale of both map backgrounds | 14d6821 | public/assets/images/levels/world/thumbnail.png, public/assets/images/levels/dungeon_1/thumbnail.png |
| 2 | Human-verify rendering in LobbyScene + LoadingScene cinematic (auto-approved) | (no commit -- verification only) | (no files) |

## Key Decisions

- **Pre-run by orchestrator.** ImageMagick downscale ran outside the agent: `magick <src> -filter Point -resize 96x64^ -gravity center -extent 96x64 <dst>` for each map. Both outputs verified `PNG 96x64` via `magick identify` before the agent committed.
- **Pure asset swap, zero code.** The plan's `acceptance_criteria` for "no code modified" was satisfied trivially -- `git diff --name-only HEAD -- src/ public/assets/data/` returned empty. All four expected `thumbnail.png` references across `src/scenes/preload-scene.ts` (lines 37, 41) and `src/scenes/lobby-scene.ts` (lines 51, 55) remain intact.
- **Human-verify auto-approved.** User explicitly authorized auto-finalization of the Task 2 checkpoint; they will catch any rendering issue in next play session and re-open Plan 05 with a follow-up if visual QA fails. Not documented as a deviation since auth gates / human approvals are normal-flow checkpoints.

## Deviations from Plan

**1. [Rule 3 - Convention] Used `.planning/todos/done/` instead of `.planning/todos/completed/`**
- **Found during:** Task 8 (final docs commit prep)
- **Issue:** Task spec said to create `.planning/todos/completed/`, but the repo already uses `.planning/todos/done/` as the established convention (contains `2026-03-27-pivot-game-to-pvp-event-multiplayer-with-google-accounts-and-ranking.md`).
- **Fix:** Moved both folded todos into the existing `done/` directory instead of creating a parallel `completed/` directory. Preserves repo convention; functionally equivalent.
- **Files modified:** `.planning/todos/pending/2026-05-21-menu-and-lobby-motion-transitions.md` -> `.planning/todos/done/`, same for `2026-05-21-replace-map-thumbnail-placeholders.md`.
- **Commit:** (final docs commit)

## Authentication Gates

None.

## Verification Results (must_haves)

### Truths

- `public/assets/images/levels/world/thumbnail.png` exists, exactly 96x64 PNG, 1244 B -- verified by `magick identify` (orchestrator) and `ls -la` (agent). File size well under 50 KB upper bound.
- `public/assets/images/levels/dungeon_1/thumbnail.png` exists, exactly 96x64 PNG, 928 B -- same verification path.
- Both PNGs produced via nearest-neighbor downscale (`-filter Point`) of the corresponding `*_background.png` sources -- confirmed by orchestrator's `magick` invocation.
- PreloadScene loads at `src/scenes/preload-scene.ts:37,41` and LobbyScene loads at `src/scenes/lobby-scene.ts:51,55` resolve to these PNG paths -- verified via Grep (4 occurrences across 2 files, exact match to plan expectation).
- LobbyScene map-browser row will pick up the real pixel-art previews via the existing `ASSET_KEYS.MAP_THUMB_*` texture wiring -- inherited from Phase 9.1-01 with no source drift.

### Artifacts

- `public/assets/images/levels/world/thumbnail.png` (96x64, 1244 B) -- provides Open Field map preview.
- `public/assets/images/levels/dungeon_1/thumbnail.png` (96x64, 928 B) -- provides Dungeon map preview.

### Key Links

- `src/scenes/lobby-scene.ts` lines 51, 55 -- `'assets/images/levels/{world,dungeon_1}/thumbnail.png'` paths present (Grep verified).
- `src/scenes/preload-scene.ts` lines 37, 41 -- same two paths present (Grep verified).
- `src/scenes/loading-scene.ts:136` -- `MAP_POOL.find(m => m.id === mapId)` present; cinematic resolves `mapEntry.thumbnailKey` to the new PNGs through the unchanged ASSET_KEYS layer (Plan 09.2-04 wiring).

## Build & Test Results

No build or test commands were run -- this is a pure binary asset swap with zero TypeScript/JavaScript surface change. `git diff --name-only HEAD -- src/ public/assets/data/` returns empty, so no code paths exercised by this plan require validation.

## Known Stubs

None. The two PNGs are the final pixel-art assets specified by Phase 9.2 success criterion #3.

## Threat Flags

None. Asset-only change at an existing static-content path served same-origin by Vite. No new endpoints, no auth surface, no schema/trust-boundary changes.

## Deferred Issues

None.

## Self-Check: PASSED

**Files exist:**
- public/assets/images/levels/world/thumbnail.png (1244 B, PNG 96x64)
- public/assets/images/levels/dungeon_1/thumbnail.png (928 B, PNG 96x64)

**Commits exist:**
- 14d6821 feat(09.2-05): add 96x64 pixel-art map thumbnails (nearest-neighbor downscale)

**Acceptance grep checks:** 4/4 OK (`thumbnail.png` references at preload-scene.ts:37,41 and lobby-scene.ts:51,55; `MAP_POOL.find` at loading-scene.ts:136). `git diff --name-only HEAD -- src/ public/assets/data/` returns empty -- no code touched.
