# Phase 9.2: UI Motion & Lobby Polish - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `09.2-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-05-21
**Phase:** 09.2-ui-motion-lobby-polish
**Areas discussed:** Music source / Gate screen status, UI-branch integration strategy, Port scope, Transition design, Multi-client sync, Typewriter implementation, LOADING timing, Joke/tip file format, Music drop sync, Thumbnail source, Menu → LobbyScene routing, Music flow + lobby ducking

---

## Music & Gate Screen Status (preconditions)

| Option | Selected |
|--------|----------|
| Music not added yet — add in this phase | |
| Music on another branch — merge first | ✓ (UI branch) |
| Music as stub now, swap later | |

| Option | Selected |
|--------|----------|
| Add a new pre-lobby gate screen in 9.2 | |
| Gate already on main/UI branch — merge it | ✓ |
| Add a one-frame overlay on Connect view | |

**User's quote:** "I think its in UI branch, you can merge it here, it may be already in main as well" / "It's sitting in main/UI branch, you may merge them instead of doing the work here"
**Notes:** Verified `UI` branch contains `splash-scene.ts`, `main-menu-scene.ts`, `music-manager.ts`, `menu_music.ogg`, `gameplay_music.ogg`. Merge base is `fe5e39e` (pre-Phase-7), so a clean merge isn't possible — pivoted to conflict-resolution planning.

---

## UI-Branch Integration Strategy

| Option | Selected |
|--------|----------|
| Port assets only — keep this branch's scene flow intact | |
| Cherry-pick the 4 UI commits, resolve conflicts manually | ✓ |
| Reverse approach — rebuild fresh, UI as reference only | |

**User's quote:** "I dont want to lose any progress that i did in the UI branch, so I think resolving the conflicts would be better? Can you resolve them?"

---

## Port Scope

| Option | Selected |
|--------|----------|
| Minimum for 9.2: main-menu + splash + music-manager + transition helper + assets | |
| Port menu flow + stub account/options/credits | |
| Port menu flow + new tilesets/maps + everything UI added | ✓ |

**User's quote:** "merge everything thats in UI branch I guess, so we have them already"
**Notes:** Reconciled with the hard reality that some UI-branch files (lobby-scene.ts, network-manager.ts, loading-scene.ts deletion, test deletions) are incompatible with multiplayer phases 7-9.1. CONTEXT.md D-02 documents the additive-from-UI / canonical-from-multiplayer policy.

---

## A. Lobby Start → LOADING Transition Design

| Option | Selected |
|--------|----------|
| Camera fade + music duck (UI's startScene pattern) | (superseded by user vision) |
| Panels slide off + screen darken + whoosh | |
| Hybrid panels + fade + duck | |

**User's quote (paraphrased):** Full cinematic LOADING flow — black background, typewriter map name, fade-in map preview, typewriter map info, fade-out to random funny sentence (joke/tip/combo array populated by user + colleague), zoom-out → zoom-in to player default zoom, 3-2-1-Start countdown, music synced to first climax at game-start.
**Resolution:** Scope reshaped to absorb a multi-stage cinematic. Lobby→LOADING handoff itself stays simple (camera fade + music stop). The new beats live **inside** LoadingScene. See D-07..D-14.

---

## Multi-Client Sync for the Transition

| Option | Selected |
|--------|----------|
| Each client plays independently when lobby:started arrives | ✓ |
| Server delays lobby:started broadcast | |
| New lobby:starting handshake event before lobby:started | |

---

## Scope Decision

| Option | Selected |
|--------|----------|
| Reshape Phase 9.2 to include the full cinematic flow | ✓ |
| Split: keep 9.2 narrow, add Phase 9.3 for LOADING cinematic | |
| Reshape 9.2 + formally re-open Phase 7/8 | |

---

## Typewriter Implementation

| Option | Selected |
|--------|----------|
| Port netgfx/Phaser-typewriter into local helper | |
| Build typewriter from scratch with Phaser tweens | ✓ |
| Add netgfx as npm dep | |

---

## LOADING Cinematic Timing Budget

| Option | Selected |
|--------|----------|
| ~8s: 1.5 + 1.0 + 2.0 + 0.5 + 2.5 + 0.5 | ✓ (with tunable constants) |
| ~6s tighter | |
| ~10s slowest | |

**User's quote:** "Maybe make it that I can tweek the variables easily so that I find the best timing, but go with the 8 seconds total for now"
**Notes:** Captured in D-10 as named constants at top of `loading-scene.ts`.

---

## Joke / Tip Source Location

| Option | Selected |
|--------|----------|
| Plain text `content/loading-tips.txt`, one line per tip | ✓ |
| Typed TS array `src/common/loading-tips.ts` | |
| JSON file `public/assets/data/loading-tips.json` | |

---

## Music Drop Sync

| Option | Selected |
|--------|----------|
| Hardcoded drop offset constant, tune by ear | ✓ |
| Server-driven music:start event | |
| No drop sync — just loop gameplay music | |

---

## B. Map Thumbnail Source

| Option | Selected |
|--------|----------|
| Downscale UI's *_background.png with ImageMagick -filter Point | ✓ |
| Hand-pixel two new 96×64 PNGs | |
| AI-gen + palette quantize | |

---

## C. Main-Menu → LobbyScene Routing

| Option | Selected |
|--------|----------|
| Keep UI menu items; CRIAR + ENTRAR both go to LobbyScene Connect view | ✓ |
| Single PLAY item replaces CRIAR/ENTRAR | |
| Wire CRIAR/ENTRAR as thin shells over NetworkManager | |

---

## E. Music Handoff Across Scenes

| Option | Selected |
|--------|----------|
| Menu music continuous through lobby + LOADING; gameplay at LOADING→GameScene | (close but adjusted) |
| Menu music ends at lobby Start; LOADING silent; gameplay at GameScene | |
| Cross-fade to gameplay during Start transition | |

**User's quote:** "I feel like the menu music should play until the host presses start, after start we sync the gameplay music with the loading/gameplay"
**Resolution:** Menu music plays splash → menu → lobby views (ducked in lobby), stops at Start transition. Gameplay music starts inside LoadingScene at the precomputed offset so its drop coincides with COUNTDOWN-end. See D-13, D-14.

---

## Lobby-View Music Volume Behavior

| Option | Selected |
|--------|----------|
| Keep menu music at fixed volume throughout | |
| Duck to lower volume in lobby views (0.05 → 0.03) | ✓ |
| Cut menu music on Connect, bring back at waiting room | |

---

## Claude's Discretion

- Exact pixel positions and BitmapText sizes for typewriter / preview / tip card inside LoadingScene.
- Fallback joke set when `loading-tips.txt` fails to load.
- Tween easings on each LOADING beat (default Linear for typewriter, Quad.Out for fades).
- Whether stub OPCOES/CONTA/CREDITOS render placeholder content or empty "coming soon".

## Deferred Ideas

- Hand-pixelled or AI-generated thumbnails (downscale ships v1).
- Server-driven music sync (hardcoded offsets ship v1).
- Beat-detection / programmatic drop offset.
- Account / Options / Credits real content.
- Per-mode joke filtering, persisted last-shown joke.
- Crossfade between menu and gameplay music.
- New UI-branch tilesets — come over via merge but unused by 9.2 criteria.
- Cleanup pass for dead imports / mixed-language strings post-merge.
