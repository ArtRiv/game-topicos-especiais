---
phase: 14-core-team-deathmatch-mode
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/scenes/game-scene.ts
autonomous: true
requirements: [TDM-05]
must_haves:
  truths:
    - "The intro cinematic plays on every client: map-name banner reveals fully (no cut letters), camera starts at map center zoomed out, smoothly pans to the local player, then zooms in to play distance"
    - "The map-name banner reveal duration is scaled to the map name's character count so long names always finish before teardown"
    - "After the zoom-in, GameScene emits HUD_REVEAL so the HUD fades in (UiScene side wired in Plan 04)"
    - "The countdown digit renders the server-driven 5,4,3,2,1 ticks using the existing pop tween"
    - "Movement + spell casting unlock simultaneously on the host-authoritative COUNTDOWN -> ACTIVE gate"
  artifacts:
    - path: "src/scenes/game-scene.ts"
      provides: "extended #enterCountdownMode: banner reveal (duration scaled to name.length), camera center->pan->zoom, HUD_REVEAL emit; #onCountdownTick renders 5..1"
      contains: "HUD_REVEAL"
  key_links:
    - from: "src/scenes/game-scene.ts #enterCountdownMode"
      to: "cameras.main.setZoom(outZoom) -> pan(player) -> zoomTo(playZoom) -> EVENT_BUS.emit(HUD_REVEAL)"
      via: "pan completion callback or tweens.chain"
      pattern: "HUD_REVEAL"
    - from: "src/scenes/game-scene.ts banner reveal"
      to: "revealMs scaled to name.length"
      via: "BANNER_MS_PER_CHAR * name.length clamped to [BANNER_MIN_MS, BANNER_MAX_MS]"
      pattern: "name.length|\\.length"
---

<objective>
Upgrade the existing Phase 8 COUNTDOWN sequence into the Team Deathmatch intro cinematic (D-18) by EXTENDING `#enterCountdownMode` / `#onCountdownTick` in `game-scene.ts` (do NOT rewrite — D-20). Add: a map-name banner whose reveal duration scales to the name's character count so it always completes (D-19 — the bug is animation TIMING, not a width/mask clip); a camera that starts at map center zoomed out, smoothly pans to the local player, then zooms in to play distance; a HUD_REVEAL emit after the zoom-in so the HUD fades in; and rendering of the server-driven 5,4,3,2,1 countdown ticks using the existing pop tween. The host-authoritative COUNTDOWN->ACTIVE unlock gate stays intact (D-18 step 7).

Purpose: Deliver the polished match opening (ROADMAP Success Criterion 5) and fix the long-standing banner "cut last letters" bug at its real root (reveal animation too short).

Output: The client half of TDM-05. The server already drives the 5..1 ticks and the unlock timing (Plan 02). The UiScene side of the HUD reveal + score plate is Plan 04.

Depends on Plan 01: consumes `CUSTOM_EVENTS.HUD_REVEAL` (already registered in event-bus.ts — do NOT re-add). Runs in Wave 2 alongside Plan 02 (no shared files: Plan 02 is server/config/debug, this plan is game-scene.ts only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md
@.planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md
@.planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md
@.planning/notes/tdm-intro-cinematic-and-banner-bug.md

<interfaces>
From src/scenes/game-scene.ts (~L3604-3683 — the cinematic block to EXTEND):
```typescript
#enterCountdownMode(): void {
  this.#controls.isMovementLocked = true;
  this.#combatLocked = true;
  if (this.#player?.body) (this.#player.body as Phaser.Physics.Arcade.Body).setVelocity(0, 0);
  this.cameras.main.setZoom(0.6);                          // outZoom (wide establishing)
  this.cameras.main.zoomTo(1.0, 3000, 'Sine.easeOut');    // playZoom — insert pan() before this
  if (this.#countdownText === null) {
    this.#countdownText = this.add
      .bitmapText(centerX, centerY, 'press_start_2p', '', 32)
      .setOrigin(0.5).setScrollFactor(0).setDepth(1000).setTint(0xffdd55);
  }
  this.#countdownText.setVisible(true).setText('');
}

#onCountdownTick = (payload: MatchCountdownTickPayload): void => {
  if (this.#countdownText === null) return;
  this.#countdownText.setText(payload.label);   // server now sends 5,4,3,2,1
  this.tweens.add({ targets: this.#countdownText, scale: { from: 1.3, to: 1.0 }, duration: 250, ease: 'Back.easeOut' });
};

#exitCountdownMode(): void { /* unlock movement + casting on COUNTDOWN -> ACTIVE (keep) */ }
// #onMatchStateChanged drives enter/exit; #combatLocked + #controls.isMovementLocked are the locks
```
</interfaces>

<read_first_skills>
MANDATORY before touching the cinematic camera/banner:
- skills/cameras/SKILL.md (camera.pan / zoomTo sequencing, completion callbacks)
- skills/tweens/SKILL.md (tweens.chain, addCounter, onComplete ordering, easing names)
- skills/text-and-bitmaptext/SKILL.md (BitmapText setText incremental reveal / addCounter typewriter)
</read_first_skills>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Map-name banner with reveal duration scaled to name length (D-19 fix)</name>
  <files>src/scenes/game-scene.ts</files>
  <read_first>
    - src/scenes/game-scene.ts (read #enterCountdownMode / #exitCountdownMode / #onCountdownTick ~L3604-3683 and #setupCamera ~L3619-3621 for the late-joiner zoom rule; how the active map id / display name is resolved in this scene — search for mapId / displayName / MAP_POOL)
    - skills/text-and-bitmaptext/SKILL.md (BitmapText reveal patterns)
    - skills/tweens/SKILL.md (addCounter / onComplete)
    - .planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md (section "src/scenes/game-scene.ts" + the press_start_2p 32px banner pattern)
    - .planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md (Surface 1 → Banner: 32px Display, y≈80-100, the D-19 fix spec with BANNER_MS_PER_CHAR≈70 clamp 600-2000)
    - .planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md (D-18 step 1, D-19, D-20)
    - .planning/notes/tdm-intro-cinematic-and-banner-bug.md (the root-cause analysis)
  </read_first>
  <action>
    Add a map-name banner to the cinematic. Resolve the active map's display name (use MAP_POOL `displayName` for the loaded `mapId`, uppercased — e.g. `WORLD`, `DUNGEON 1`, `STAGES`; read how the scene currently knows its mapId).

    Create the banner as a `press_start_2p` BitmapText: 32px, `setOrigin(0.5)`, `setScrollFactor(0)`, `setDepth(1000)`, tint `0xffdd55` (gold, matching the countdown digit), positioned horizontally centered at y ≈ 90 (upper third; must NOT overlap the centered countdown digit at y=160). Store the ref on a private field `#mapBanner` so it can be torn down.

    **D-19 BANNER FIX (the whole point):** the banner "cuts the last letters" because the reveal animation is too SHORT to finish before teardown — NOT a width/origin/mask clip. Implement a typewriter reveal whose DURATION scales to character count:
    ```typescript
    const name = mapDisplayName.toUpperCase();
    const BANNER_MS_PER_CHAR = 70;   // (tunable)
    const BANNER_MIN_MS = 600;       // (tunable)
    const BANNER_MAX_MS = 2000;      // (tunable)
    const revealMs = Phaser.Math.Clamp(BANNER_MS_PER_CHAR * name.length, BANNER_MIN_MS, BANNER_MAX_MS);
    ```
    Drive the reveal with `this.tweens.addCounter({ from: 0, to: name.length, duration: revealMs, ease: 'Linear', onUpdate: (tw) => this.#mapBanner.setText(name.slice(0, Math.round(tw.getValue()))) })`. The banner fade-out (teardown) MUST begin ONLY in the tween's `onComplete` (so long names always complete first). Do NOT widen any container, change origin, or add a mask — those are the red herrings the note warns against.

    Banner teardown: in `onComplete`, hold briefly then fade out (`alpha 1->0`, ~300ms) and `destroy()`/null the ref. The banner is gone by the time the HUD reveals (Task 2 step 5).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenes/game-scene.ts` creates a `press_start_2p` banner BitmapText at 32px with the uppercased map display name
    - the banner reveal duration references `name.length` (grep for `.length` near a banner/reveal/BANNER_MS_PER_CHAR usage) — proves the D-19 TIMING fix, not a width change
    - the file contains `BANNER_MS_PER_CHAR` (or an equivalent per-char duration constant)
    - the banner fade-out is triggered from the reveal tween's `onComplete` (not on a fixed timer independent of reveal)
    - no `setMask`/container-width change was introduced for the banner
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>The map-name banner reveals via a typewriter whose duration scales to the name length and clamps to [600,2000]ms; teardown starts only after the reveal completes, so long names never get cut. Frontend builds clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Camera center->pan->zoom sequence + HUD_REVEAL emit + 5..1 tick rendering</name>
  <files>src/scenes/game-scene.ts</files>
  <read_first>
    - src/scenes/game-scene.ts (the #enterCountdownMode camera block ~L3604-3683, the #onCountdownTick pop tween ~L3673-3683, #exitCountdownMode unlock ~L3660-3664, the SHUTDOWN cleanup block ~L3702-3719; how to get the world/map center and the local player position)
    - skills/cameras/SKILL.md (cameras.main.centerOn / pan / zoomTo, completion callbacks, scrollX/Y)
    - skills/tweens/SKILL.md (tweens.chain, sequencing, easing)
    - .planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md (section "src/scenes/game-scene.ts" — the camera block + the late-joiner rule + SHUTDOWN cleanup note)
    - .planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md (Surface 1 → Camera ranges: outZoom 0.5-0.65, playZoom 1.0, center hold 300-600ms, pan 900-1400ms Sine.easeInOut, zoom-in 700-1100ms Cubic.easeOut; UI reveal step 5 via HUD_REVEAL)
    - .planning/phases/14-core-team-deathmatch-mode/14-01-PLAN.md (confirms CUSTOM_EVENTS.HUD_REVEAL is registered — do NOT re-add to event-bus.ts)
    - .planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md (D-18 steps 2-7, D-20)
  </read_first>
  <action>
    Extend `#enterCountdownMode` to play the D-18 camera sequence (D-20: extend, exact values are discretion within the UI-SPEC ranges). Order:

    1. Set the wide establishing view: `this.cameras.main.setZoom(outZoom)` with `outZoom = 0.6` (reuse the existing snap-out value), and center the camera on the MAP center (`this.cameras.main.centerOn(mapCenterX, mapCenterY)` — compute from the tilemap dimensions; read how the scene knows map width/height). This `setZoom(outZoom)` stays ONLY inside the cinematic, never in `#setupCamera` (late-joiner safety — a client that misses COUNTDOWN must default to play zoom; keep this existing rule).
    2. Hold ~400ms (center hold), then pan to the local player: `this.cameras.main.pan(this.#player.x, this.#player.y, 1100, 'Sine.easeInOut')`.
    3. On pan completion, zoom in: `this.cameras.main.zoomTo(1.0, 900, 'Cubic.easeOut')`. Sequence pan->zoom via the pan completion callback (`cameras.main.once('camerapancomplete', ...)`) or `tweens.chain` — see skills/cameras/SKILL.md.
    4. On zoom-in completion, emit the HUD reveal: `EVENT_BUS.emit(CUSTOM_EVENTS.HUD_REVEAL)` (the UiScene fades its HUD container in — wired in Plan 04). Use `cameras.main.once('camerazoomcomplete', ...)` or the zoomTo onComplete.

    Replace the current single `setZoom(0.6) + zoomTo(1.0, 3000)` pair with this center->hold->pan->zoom chain. Keep the `#combatLocked = true` + `isMovementLocked = true` + velocity-zero at the top of `#enterCountdownMode` unchanged (players stay locked the whole sequence).

    **Countdown ticks:** `#onCountdownTick` already does `setText(payload.label)` + the pop tween — the server now sends `5,4,3,2,1` (Plan 02), so NO client change is needed beyond confirming the existing pop tween (`scale {from:1.3,to:1.0}, duration:250, ease:'Back.easeOut'`) is preserved verbatim. Do NOT add a client-side setInterval; labels are server-driven.

    **Unlock gate (D-18 step 7):** `#exitCountdownMode` (triggered by the host COUNTDOWN->ACTIVE broadcast via `#onMatchStateChanged`) still unlocks movement + casting simultaneously. Keep it. Ensure the banner + any cinematic-only tweens are cleaned up by then (banner self-tears-down in Task 1; cancel any in-flight pan/zoom if the match transitions early).

    **SHUTDOWN cleanup (~L3702-3719):** stop/remove any new tween or camera listener (`camerapancomplete` / `camerazoomcomplete` `once` handlers auto-remove, but kill the banner ref + any held tween) so a scene restart does not leak. Do NOT add an EVENT_BUS.on here (this plan only EMITs HUD_REVEAL; it does not subscribe).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenes/game-scene.ts` #enterCountdownMode calls `cameras.main.pan(` toward the local player
    - the camera sequence orders center/hold -> pan -> zoomTo(1.0, ...) (pan precedes zoom-in)
    - `src/scenes/game-scene.ts` emits `CUSTOM_EVENTS.HUD_REVEAL` after the zoom-in completes (grep for `HUD_REVEAL`)
    - the outZoom `setZoom(0.6)` is only inside the cinematic, not in #setupCamera (grep #setupCamera shows no 0.6 zoom)
    - `#onCountdownTick` still uses `scale: { from: 1.3, to: 1.0 }, duration: 250, ease: 'Back.easeOut'` (pop tween preserved)
    - no client-side `setInterval` drives countdown labels
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>The cinematic plays center(wide) -> hold -> pan-to-player -> zoom-in -> HUD_REVEAL; the 5..1 server ticks render with the preserved pop tween; movement + casting unlock on the host COUNTDOWN->ACTIVE gate; cinematic teardown is clean. Frontend builds clean.</done>
</task>

</tasks>

<verification>
- `pnpm build` clean (frontend type-check + Vite build)
- Banner reveal duration scales to name length (D-19 fix — TIMING, not width)
- Camera sequence: map-center wide -> pan to player -> zoom to play distance
- HUD_REVEAL emitted after zoom-in (consumed by Plan 04 UiScene)
- 5..1 ticks rendered via the preserved pop tween
- COUNTDOWN->ACTIVE unlock gate intact
</verification>

<success_criteria>
- ROADMAP Success Criterion 5 (TDM-05, client half): the intro cinematic plays on every client (banner always completes, camera center->pan->zoom, UI reveal trigger, 5..1 countdown, simultaneous unlock on the host-authoritative gate). The banner cut-letters bug is fixed at its real root.
</success_criteria>

<output>
After completion, create `.planning/phases/14-core-team-deathmatch-mode/14-03-SUMMARY.md`. Note for Plan 04: GameScene emits `CUSTOM_EVENTS.HUD_REVEAL` after the cinematic zoom-in; UiScene must subscribe and fade its `#hudContainer` (alpha 0->1) in response, and must START with the HUD hidden during the cinematic. Record the chosen banner field name (`#mapBanner`) and any new private camera-sequence fields so Plan 04 does not collide.
</output>
