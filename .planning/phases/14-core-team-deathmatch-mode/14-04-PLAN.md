---
phase: 14-core-team-deathmatch-mode
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-03]
files_modified:
  - src/scenes/ui-scene.ts
  - src/scenes/game-scene.ts
  - src/scenes/tdm-results-scene.ts
  - src/scenes/scene-keys.ts
  - src/main.ts
autonomous: true
requirements: [TDM-06, TDM-04, TDM-03]
must_haves:
  truths:
    - "A top-center team-score plate reads [A] n - m [B] with each team's score tinted its lobby team color"
    - "On every score broadcast the changed number pops with the countdown-tick tween shape"
    - "The HUD (incl. the score plate AND the radial-menu/element affordance) starts hidden during the cinematic and fades in on HUD_REVEAL"
    - "A respawned local player's sprite alpha-pulses while invulnerable and the pulse stops on first move, first cast, or the max-duration cap"
    - "On match ENDED a minimal results screen shows the winning team, a per-player kills/deaths table, an MVP highlight, and a working RETURN TO LOBBY button"
  artifacts:
    - path: "src/scenes/ui-scene.ts"
      provides: "top-center team-score plate in #hudContainer + score-pop tween + HUD_REVEAL fade-in (covers element/radial affordance) + TEAM_COLORS"
      contains: "NETWORK_TEAM_SCORE"
    - path: "src/scenes/game-scene.ts"
      provides: "respawn invuln alpha-pulse on local player, cancel on move/cast/timeout"
      contains: "RESPAWN_INVULN_MAX_MS"
    - path: "src/scenes/tdm-results-scene.ts"
      provides: "new results scene: winner line, K/D table, MVP, RETURN TO LOBBY button"
      contains: "RETURN TO LOBBY"
    - path: "src/scenes/scene-keys.ts"
      provides: "TDM_RESULTS_SCENE key"
      contains: "TDM_RESULTS_SCENE"
    - path: "src/main.ts"
      provides: "TdmResultsScene registered in the scene list"
      contains: "TdmResultsScene"
  key_links:
    - from: "src/scenes/ui-scene.ts"
      to: "EVENT_BUS NETWORK_TEAM_SCORE + HUD_REVEAL"
      via: "EVENT_BUS.on with matching SHUTDOWN off"
      pattern: "NETWORK_TEAM_SCORE"
    - from: "src/scenes/game-scene.ts #onRespawn local branch"
      to: "invuln alpha-pulse tween (stop on move/cast/timeout)"
      via: "looping yoyo alpha tween + cancel hooks"
      pattern: "RESPAWN_INVULN_MAX_MS"
    - from: "src/scenes/tdm-results-scene.ts"
      to: "EVENT_BUS NETWORK_MATCH_ENDED -> render stats"
      via: "scene launched on ENDED, reads MatchEndedPayload"
      pattern: "NETWORK_MATCH_ENDED"
---

<objective>
Deliver the remaining client UI: the top-center team-score HUD plate (D-15/D-16/D-17), the respawn invulnerability blink + cancel-on-move/cast cue (D-12/D-13 client half), and the minimal results screen on match ENDED (D-06/D-07/D-08). Also wire the UiScene side of the cinematic HUD reveal (subscribe to HUD_REVEAL, start the HUD hidden) — and per D-18 step 5 / UI-SPEC Surface 1 the reveal must cover the radial-menu/element affordance too, not just the bars.

Purpose: Players need to see the live team score, get the fair-reentry visual signal on respawn, and see who won + per-player K/D + MVP at match end with a way back to the lobby. The cinematic's "UI reveals — radial menu, mana, HP" beat (D-18 step 5) is only met if the element-gem/radial affordance reveals together with the bars.

Output: TDM-06 (score plate + results screen), TDM-04 client cue (invuln blink + cancel), TDM-03 client-side map display (score plate consumes team colors; results consumes per-player stats).

Depends on Plan 01 (CUSTOM_EVENTS.NETWORK_TEAM_SCORE / NETWORK_MATCH_ENDED / HUD_REVEAL + payload types) and Plan 03 (shares game-scene.ts cinematic edits + the #mapBanner/HUD-hidden state). Wave 3 — runs after both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md
@.planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md
@.planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md
@.planning/phases/14-core-team-deathmatch-mode/14-01-SUMMARY.md
@.planning/phases/14-core-team-deathmatch-mode/14-03-SUMMARY.md

<interfaces>
From src/networking/types.ts (added in Plan 01):
```typescript
export type TdmPlayerStat = { playerId: string; name: string; team: number; kills: number; deaths: number; };
export type TeamScorePayload = { teamScores: [number, number]; lastScoringTeam: number; };
export type MatchEndedPayload = { winningTeam: number | null; teamScores: [number, number]; mvpPlayerId: string | null; stats: TdmPlayerStat[]; };
```

From src/common/event-bus.ts (CUSTOM_EVENTS — added in Plan 01):
```typescript
NETWORK_TEAM_SCORE: 'NETWORK_TEAM_SCORE',
NETWORK_MATCH_ENDED: 'NETWORK_MATCH_ENDED',
HUD_REVEAL: 'HUD_REVEAL',
```

From src/scenes/ui-scene.ts (VERIFIED current state):
```typescript
// #hudContainer (this.add.container at ~L122) holds: hearts (added ~L146), mana bar + text (added ~L167),
//   and ONLY the #windDashHint bitmapText at (240,308) 6px (added ~L203).
// IMPORTANT: the element affordance is NOT in #hudContainer today —
//   #elementGem (this.add.arc, ~L172), #elementLabel (this.add.text 'FIRE', ~L173),
//   #elementHintText (this.add.text '[SPACE]', ~L180), and #createElementCarousel() (~L188)
//   are added straight to the scene, so they will NOT fade with #hudContainer.
//   → This plan must add them to #hudContainer so HUD_REVEAL covers the radial/element affordance (D-18 step 5).
// EVENT_BUS.on(...) listeners at ~L206-213 paired with off at ~L215-224 (SHUTDOWN)
// cooldown fade-in tween ~L412-413 is the in-scene tween precedent
// top-center band x 100-380, y 4-20 is FREE — score plate goes at center-X 240, y≈8-12
```

From src/scenes/game-scene.ts:
```typescript
// #onRespawn local branch ~L3541-3558: clears death, setPosition, lifeComponent.resetToFull, clearTint
//   → ADD invuln pulse here (Plan 03 left #mapBanner + HUD-hidden state; do not collide)
// Player.iFrameUntil (player.ts:59), invulnerableComponent (invulnerable-component.ts)
// cast/move entry points to add cancel hooks: ~L1910-1911, 1995-1996, 2640-2642 (combatLocked/isMovementLocked checks)
// SHUTDOWN cleanup ~L3702-3719
```

From src/scenes/scene-keys.ts:
```typescript
export const SCENE_KEYS = { /* ... */ GAME_OVER_SCENE: 'GAME_OVER_SCENE', /* ... */ } as const;
```

From src/scenes/game-over-scene.ts (scene-shell analog) + src/scenes/main-menu-scene.ts (~L285-310 button hover pattern) — read both.
</interfaces>

<read_first_skills>
MANDATORY before touching the score plate text / results scene:
- skills/text-and-bitmaptext/SKILL.md (BitmapText per-tint pieces, setText, sizing)
- skills/scenes/SKILL.md (scene launch/start/stop lifecycle, parallel vs overlay, SHUTDOWN)
</read_first_skills>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Team-score HUD plate in UiScene + HUD_REVEAL fade-in (incl. radial/element affordance) + TEAM_COLORS</name>
  <files>src/scenes/ui-scene.ts</files>
  <read_first>
    - src/scenes/ui-scene.ts (read the #hudContainer build: container create ~L122, hearts add ~L146, mana bar add ~L167, the element block #elementGem/#elementLabel/#elementHintText ~L169-186, #createElementCarousel ~L188, the windDashHint add ~L196-203; the EVENT_BUS on/off block ~L206-224; the cooldown fade tween ~L412-413; #setElementColor at ~L304)
    - src/scenes/lobby-scene.ts (the team BADGE tint constants ~L1495: team 0 -> 0x44aaff, team 1 -> 0xff5533 — hoist these, NOT the toggle-button fills 0x0066dd/0xcc2200)
    - skills/text-and-bitmaptext/SKILL.md
    - .planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md (section "src/scenes/ui-scene.ts" — collision anchors, score-pop tween, EVENT_BUS pattern, TEAM_COLORS hoist)
    - .planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md (Surface 1 "radial menu, mana, HP reveal" + Surface 2 full spec: center-X 240, y≈8-12, [A] n - m [B], per-piece tint, pop tween, re-center on digit growth)
    - .planning/phases/14-core-team-deathmatch-mode/14-03-SUMMARY.md (HUD_REVEAL contract; HUD must start hidden)
    - .planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md (D-15, D-16, D-17, D-18 step 5)
  </read_first>
  <action>
    1. Hoist a shared `export const TEAM_COLORS = [0x44aaff, 0xff5533] as const;` (Team A cyan-blue, Team B red-orange — the lobby BADGE tints, NOT the toggle fills). Put it where other scene-shared constants live (or top of ui-scene.ts and re-export); update lobby-scene.ts to read from it ONLY if trivial — otherwise just define it here and leave the lobby literal (do NOT risk breaking lobby in this plan; a comment noting the source is enough).

    2. Build the score plate as SEPARATE BitmapText pieces (per-piece tint, D-16), all `press_start_2p` 16px, added to `this.#hudContainer` so they reveal/hide with the HUD:
       - `[A]` bracket + Team A number → tint `TEAM_COLORS[0]` (0x44aaff)
       - `–` separator → white `0xffffff`
       - Team B number + `[B]` bracket → tint `TEAM_COLORS[1]` (0xff5533)
       Lay them out left->right around center-X 240, y ≈ 10, ~4-8px gaps, the group centered as one unit. Store refs (`#scoreTextA`, `#scoreDash`, `#scoreTextB` or similar). Must NOT overlap mana bar (x=8,y=14) or hearts (x 157-237, y 25/33) — the top-center band is free.

    3. **Ensure the radial-menu / element affordance reveals with the HUD (D-18 step 5, UI-SPEC Surface 1).** The element affordance is NOT currently in `#hudContainer` (verified: `#elementGem`, `#elementLabel`, `#elementHintText`, and the element carousel are added straight to the scene at ~L169-188; only `#windDashHint` is added to the container). Add these element/radial affordance objects to `this.#hudContainer` (e.g. `this.#hudContainer.add([this.#elementGem, this.#elementLabel, this.#elementHintText])` and add the carousel object(s) returned/owned by `#createElementCarousel`) so the HUD_REVEAL fade-in in step 5 covers the radial-menu affordance, not just the bars. Confirm nothing breaks `#setElementColor` (~L304) — those refs still point to the same objects, only their parent changes. OPTIONAL (UI-SPEC): add a 6px `press_start_2p` "Q/E SWITCH ELEMENT" hint near the element gem, mirroring the existing 6px "SHIFT  WIND DASH" hint at (240,308); if added, it MUST also be added to `#hudContainer`.

    4. Subscribe to `CUSTOM_EVENTS.NETWORK_TEAM_SCORE` (TeamScorePayload). On each event: `setText` the changed numbers (`teamScores[0]`, `teamScores[1]`), re-center the group if a digit count grew (e.g. 9->10), and play the pop tween ONLY on the number for `payload.lastScoringTeam`: `this.tweens.add({ targets: <that number obj>, scale: { from: 1.3, to: 1.0 }, duration: 250, ease: 'Back.easeOut' })` (identical to the countdown-tick shape, D-17).

    5. Subscribe to `CUSTOM_EVENTS.HUD_REVEAL`: the `#hudContainer` must START hidden (set `#hudContainer.setAlpha(0)` at create — the cinematic owns visibility; because steps 2-3 put both the score plate AND the element/radial affordance inside the container, one alpha controls the whole HUD reveal). On HUD_REVEAL, fade in: `this.tweens.add({ targets: this.#hudContainer, alpha: { from: 0, to: 1 }, duration: 280, ease: 'Quad.easeOut' })`. (If the HUD was already visible because no cinematic ran — e.g. a late joiner — guard so HUD_REVEAL just sets alpha 1; also defensively reveal on the ACTIVE state if HUD_REVEAL is never received.)

    6. Pair EVERY new `EVENT_BUS.on` with a matching `EVENT_BUS.off` inside the existing SHUTDOWN block (~L215-224), mirroring the MANA_UPDATED pattern.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenes/ui-scene.ts` defines/uses `TEAM_COLORS` = `[0x44aaff, 0xff5533]`
    - the score plate is built from separate BitmapText pieces added to `#hudContainer`
    - the element/radial affordance (#elementGem + element label/hint + carousel) is added to `#hudContainer` so it is covered by the HUD_REVEAL fade-in (D-18 step 5)
    - `src/scenes/ui-scene.ts` subscribes to `CUSTOM_EVENTS.NETWORK_TEAM_SCORE` and `CUSTOM_EVENTS.HUD_REVEAL`, each with a matching SHUTDOWN `off`
    - the score-pop tween uses `scale: { from: 1.3, to: 1.0 }, duration: 250, ease: 'Back.easeOut'`
    - `#hudContainer` starts at alpha 0 and fades to 1 on HUD_REVEAL
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>The top-center [A] n - m [B] plate renders with per-team tints, pops the changed number on each broadcast, lives in #hudContainer (hidden during cinematic, fades in on HUD_REVEAL) ALONGSIDE the radial-menu/element affordance so step 5's "radial menu, mana, HP" reveal is fully met, and cleans up its listeners on SHUTDOWN. Frontend builds clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Respawn invuln alpha-pulse on local player; cancel on move/cast/timeout</name>
  <files>src/scenes/game-scene.ts</files>
  <read_first>
    - src/scenes/game-scene.ts (read #onRespawn ~L3541-3558 esp. the isLocal branch; the cast/move entry points ~L1910-1911, 1995-1996, 2640-2642; #handleDashInput / update() input flow; SHUTDOWN ~L3702-3719; any #mapBanner / cinematic fields Plan 03 added — read the 14-03-SUMMARY for field names to avoid collision)
    - src/components/state-machine/states/character/hurt-state.ts (~L72-90 the i-frame blink precedent: set invulnerable + delayedCall)
    - src/components/game-object/invulnerable-component.ts (~L4-25)
    - src/common/runtime-config.ts (RESPAWN_INVULN_MAX_MS — read it from RUNTIME_CONFIG)
    - skills/tweens/SKILL.md (yoyo/repeat alpha tween, stop(), tween refs)
    - .planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md (section "Invuln blink contract (D-13)" — sustained looping yoyo, stop on first of move/cast/timeout, hard alpha=1)
    - .planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md (Surface 3 full spec: alpha 1.0<->0.35, yoyo, repeat -1, 150ms/half, stop + alpha=1 on end)
    - .planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <action>
    Add a respawn invulnerability blink to the LOCAL player, started in the `isLocal` branch of `#onRespawn` (AFTER `#clearLocalDeath` tears down the death overlay + position/HP/tint are restored — do not blink while the death overlay is up).

    1. Add private fields: `#invulnPulseTween: Phaser.Tweens.Tween | null = null;` and `#invulnUntil: number = 0;` (client-side mirror for the cancel logic; the SERVER is authority per D-14 — this is purely cosmetic + local gating).

    2. On local respawn, start the pulse (UI-SPEC Surface 3):
    ```typescript
    this.#invulnUntil = this.time.now + RUNTIME_CONFIG.RESPAWN_INVULN_MAX_MS;   // ~2500ms
    this.#player.setAlpha(1);
    this.#invulnPulseTween = this.tweens.add({
      targets: this.#player,
      alpha: { from: 1.0, to: 0.35 },
      duration: 150, yoyo: true, repeat: -1,
    });
    ```
    Also set the existing `this.#player.iFrameUntil = this.#invulnUntil` so the local damage gate (Phase 9.3 `#onDamageConfirmed` i-frame check) already respects it for immediate visual correctness.

    3. Add a private `#stopInvulnBlink(): void` that, if active, stops/removes `#invulnPulseTween`, nulls it, sets `this.#player.setAlpha(1)` (hard reset), and zeroes `#invulnUntil`.

    4. Cancel-on-first-of (D-12):
       - **Move:** in the movement input path (the entry points ~L1910-1911 / 1995-1996 — read which one fires when the local player produces non-zero movement input), if `#invulnUntil > 0` and the player issued real movement this frame, call `#stopInvulnBlink()`.
       - **Cast:** in the cast path (~L2640-2642 / wherever a local cast is initiated), if `#invulnUntil > 0`, call `#stopInvulnBlink()`.
       - **Timeout:** in `update()`, if `#invulnUntil > 0 && this.time.now >= #invulnUntil`, call `#stopInvulnBlink()`.
       Keep these checks cheap (guard on `#invulnUntil > 0` first so the common case is a single comparison).

    5. SHUTDOWN cleanup (~L3702-3719): call `#stopInvulnBlink()` so the looping tween never leaks across scene restarts.

    Do NOT change the server authority — the client never sends invuln state; this only mirrors it for the visual + the existing local i-frame gate. The differentiation from the hurt-blink is the sustained slow loop (UI-SPEC).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenes/game-scene.ts` starts an alpha pulse tween (`alpha` `1.0`/`0.35`, `yoyo: true`, `repeat: -1`, `duration: 150`) on local respawn
    - the file references `RESPAWN_INVULN_MAX_MS` (via RUNTIME_CONFIG) to size the window
    - a `#stopInvulnBlink` (or equivalently named) helper stops the tween and hard-sets alpha to 1
    - cancel hooks exist for move, cast, and timeout (grep for `#stopInvulnBlink` usages — at least 3 call sites + SHUTDOWN)
    - SHUTDOWN calls the stop helper
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>A respawned local player visibly pulses while invulnerable; the pulse stops the instant they move, cast, or hit the ~2.5s cap, with a hard alpha reset; no tween leaks on shutdown. Frontend builds clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Minimal TDM results scene (winner line, K/D table, MVP, RETURN TO LOBBY) + registration</name>
  <files>src/scenes/tdm-results-scene.ts, src/scenes/scene-keys.ts, src/main.ts, src/scenes/game-scene.ts</files>
  <read_first>
    - src/scenes/game-over-scene.ts (scene boilerplate ~L9-19, 44-48: constructor super({key}), create(), SHUTDOWN; scene.start back-nav)
    - src/scenes/main-menu-scene.ts (~L285-310 the menu-button hover/scale/tint pattern to copy verbatim)
    - src/scenes/scene-keys.ts (the full SCENE_KEYS object — add TDM_RESULTS_SCENE)
    - src/main.ts (~L112-115 the scene registration list — game.scene.add next to GAME_OVER_SCENE)
    - src/scenes/game-scene.ts (#onMatchStateChanged / how ENDED is observed; how to launch a scene; SHUTDOWN ~L3702-3719)
    - skills/scenes/SKILL.md (launch vs start, overlay, lifecycle)
    - skills/text-and-bitmaptext/SKILL.md
    - .planning/phases/14-core-team-deathmatch-mode/14-PATTERNS.md (section "src/scenes/tdm-results-scene.ts" — full build recipe: scrim, winner line, table rows, MVP, button, registration)
    - .planning/phases/14-core-team-deathmatch-mode/14-UI-SPEC.md (Surface 4 full layout + Copywriting + error/empty state fallbacks)
    - .planning/phases/14-core-team-deathmatch-mode/14-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <action>
    1. Add `TDM_RESULTS_SCENE: 'TDM_RESULTS_SCENE'` to `SCENE_KEYS` in `src/scenes/scene-keys.ts`.

    2. Create `src/scenes/tdm-results-scene.ts` — `export class TdmResultsScene extends Phaser.Scene` with `constructor() { super({ key: SCENE_KEYS.TDM_RESULTS_SCENE }); }`. It reads a `MatchEndedPayload` (pass via `this.scene.start(SCENE_KEYS.TDM_RESULTS_SCENE, payload)` data, OR subscribe to `CUSTOM_EVENTS.NETWORK_MATCH_ENDED` — prefer passing via scene data when GameScene launches it on ENDED; if it subscribes to the event, pair on/off in SHUTDOWN). In `create(data: MatchEndedPayload)`:
       - **Scrim:** full-screen `this.add.rectangle(0,0,480,320,0x000000,0.7).setOrigin(0,0).setScrollFactor(0)`; optional fade-in (alpha 0->1, ~200ms) — minimal animation only (D-06).
       - **Winner line:** 32px `press_start_2p` at center-X 240, y≈48: `TEAM A WINS` / `TEAM B WINS` tinted `TEAM_COLORS[winningTeam]`; `DRAW` in gold `0xffdd55` if `winningTeam == null`.
       - **Table header:** 8px `PLAYER     K    D` (dim/white) at y≈96.
       - **Player rows:** sort `stats` by team then kills-desc (or kills-desc); one 8px BitmapText per row, row pitch 16px, starting y≈116, each tinted with that player's `TEAM_COLORS[stat.team]`. Show `name`, `kills`, `deaths`. Fall back to `0` + neutral tint on missing fields (UI-SPEC error state — never error).
       - **MVP highlight:** the row whose `playerId === mvpPlayerId` gets a gold `★`/`MVP` tag prefix + a thin gold underline rect (`0xffdd55`). Tie-break already resolved server-side (mvpPlayerId is authoritative); if `mvpPlayerId == null`, skip the highlight.
       - **RETURN TO LOBBY button:** 16px `press_start_2p` at center-X 240, y≈276, copy the main-menu hover pattern verbatim: idle white, `pointerover` -> scale 1.05 (100ms Quad.Out) + gold tint, `pointerout` -> revert, `pointerup` -> navigate back to the lobby flow (`this.scene.start(SCENE_KEYS.LOBBY_SCENE)` or whatever the established return path is — read game-over-scene's back-nav).
       - SHUTDOWN: off any EVENT_BUS listeners; the scene tears down its own GameObjects automatically.

    3. Register in `src/main.ts`: import `TdmResultsScene` and add `game.scene.add(SCENE_KEYS.TDM_RESULTS_SCENE, TdmResultsScene)` (or the existing add-pattern) next to `GAME_OVER_SCENE` (~L114).

    4. Launch it on ENDED from `src/scenes/game-scene.ts`: when `#onMatchStateChanged` (or the NETWORK_MATCH_ENDED subscription) observes match end, launch the results scene with the payload. Use the established pattern (game-over-scene is launched on existing match-end — mirror it). If GameScene subscribes to `CUSTOM_EVENTS.NETWORK_MATCH_ENDED` to grab the payload, pair the `on` with an `off` in SHUTDOWN (~L3702-3719). Stop/freeze GameScene + UiScene as the existing game-over flow does (read how GAME_OVER_SCENE handles this).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/scenes/scene-keys.ts` contains `TDM_RESULTS_SCENE`
    - `src/scenes/tdm-results-scene.ts` exists, exports `TdmResultsScene`, and contains `RETURN TO LOBBY`
    - `src/main.ts` imports and registers `TdmResultsScene` in the scene list
    - the results scene renders a winner line tinted by team (or gold DRAW), a per-player K/D table, and an MVP highlight
    - the RETURN TO LOBBY button uses the menu hover pattern (scale 1.05, 100ms Quad.Out, gold tint) and navigates back to the lobby
    - `src/scenes/game-scene.ts` launches the results scene on match ENDED
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>On match ENDED a minimal results scene shows the winner, per-player kills/deaths, the MVP, and a working RETURN TO LOBBY button; the scene is registered in scene-keys.ts + main.ts and launched from GameScene on ENDED. Frontend builds clean.</done>
</task>

</tasks>

<verification>
- `pnpm build` clean
- Score plate: top-center [A] n - m [B], per-team tint, pops changed number, in #hudContainer, hidden during cinematic, fades in on HUD_REVEAL
- HUD_REVEAL fade-in covers the radial-menu/element affordance (it now lives in #hudContainer) — D-18 step 5 met
- Invuln blink: sustained alpha pulse on local respawn, stops on move/cast/timeout, hard alpha reset, no leaks
- Results scene: registered (scene-keys + main.ts), launched on ENDED, winner + K/D table + MVP + RETURN TO LOBBY
- All new EVENT_BUS.on paired with SHUTDOWN off
</verification>

<success_criteria>
- ROADMAP Success Criterion 4 (TDM-04, client half): respawn invuln has a visible cue that cancels the instant the player moves or casts (server remains authority).
- ROADMAP Success Criterion 5 (TDM-05, client UI reveal half): the cinematic HUD reveal (Plan 03 emit -> Plan 04 subscribe) reveals the radial menu, mana, and HP together (D-18 step 5), closed end-to-end.
- TDM-06: team-score HUD plate (live, team-colored, pop on change) + minimal results screen (winner, K/D table, MVP, return-to-lobby).
</success_criteria>

<output>
After completion, create `.planning/phases/14-core-team-deathmatch-mode/14-04-SUMMARY.md`. Record: the TEAM_COLORS location, the score-plate field names, the invuln field names, that the element/radial affordance was moved into #hudContainer (so it reveals with the HUD), and how the results scene receives its payload (scene data vs event subscription) so any follow-up phase (15 pickups) knows the conventions.
</output>
</content>
