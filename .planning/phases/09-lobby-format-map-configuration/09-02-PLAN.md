---
phase: 09-lobby-format-map-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/images/levels/world/thumbnail.png
  - public/assets/images/levels/dungeon_1/thumbnail.png
  - public/assets/data/assets.json
  - src/common/assets.ts
autonomous: false
requirements: [LBC-04]

must_haves:
  truths:
    - "Two 96x64 map thumbnail PNGs exist at the canonical level asset paths (LBC-04 backing assets)"
    - "PreloadScene loads both thumbnails under SCREAMING_SNAKE texture keys MAP_THUMB_WORLD and MAP_THUMB_DUNGEON_1"
    - "ASSET_KEYS exports MAP_THUMB_WORLD and MAP_THUMB_DUNGEON_1 so Plan 03's LobbyScene references them by constant, not literal"
  artifacts:
    - path: "public/assets/images/levels/world/thumbnail.png"
      provides: "Open Field map thumbnail (96x64 pixel-art PNG)"
    - path: "public/assets/images/levels/dungeon_1/thumbnail.png"
      provides: "Dungeon map thumbnail (96x64 pixel-art PNG)"
    - path: "public/assets/data/assets.json"
      provides: "MAP_THUMB_WORLD and MAP_THUMB_DUNGEON_1 image entries appended to existing levels/world and levels/dungeon_1 packs"
      contains: "MAP_THUMB_WORLD"
    - path: "src/common/assets.ts"
      provides: "ASSET_KEYS.MAP_THUMB_WORLD and ASSET_KEYS.MAP_THUMB_DUNGEON_1 constants"
      contains: "MAP_THUMB_WORLD"
  key_links:
    - from: "public/assets/data/assets.json"
      to: "PreloadScene"
      via: "this.load.pack(ASSET_PACK_KEYS.MAIN, 'assets/data/assets.json') picks up new entries automatically"
      pattern: "MAP_THUMB"
    - from: "src/common/assets.ts"
      to: "src/scenes/lobby-scene.ts (Plan 03)"
      via: "MapPreviewCard renders this.add.image(x, y, ASSET_KEYS.MAP_THUMB_WORLD)"
      pattern: "ASSET_KEYS.MAP_THUMB"
---

<objective>
Create the two map thumbnail assets and register them in the preload asset pack so Plan 03's MapPreviewCard component has textures to render. Also add the SCREAMING_SNAKE ASSET_KEYS constants Plan 03 will reference, satisfying the pattern-mapper's recommendation to reconcile UI-SPEC's kebab-case illustration with the codebase's existing convention.

Purpose: Backs LBC-04 (selected map name + preview metadata visible on every client). Thumbnails are the "preview metadata" affordance.

Output: Two PNG files on disk, two new image entries in assets.json, two new ASSET_KEYS constants. PreloadScene already loads the asset pack — no code change there. Plan 03 binds these to UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-lobby-format-map-configuration/09-CONTEXT.md
@.planning/phases/09-lobby-format-map-configuration/09-PATTERNS.md
@.planning/phases/09-lobby-format-map-configuration/09-UI-SPEC.md
@public/assets/data/assets.json
@src/common/assets.ts
@src/scenes/preload-scene.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Author the two map thumbnail PNGs</name>
  <what-built>Two new asset files must exist on disk before Tasks 2-3 register them.</what-built>
  <how-to-verify>
    The thumbnails are art assets — Claude cannot author pixel-art PNGs that match the game's visual style. Pick one of two production paths:

    Option A — User authors / sources them manually:
    1. Create a 96x64 PNG depicting the Open Field map (re-use a downscaled crop of `public/assets/images/levels/world/world_background.png` or hand-pixel a representative tile arrangement). Save as `public/assets/images/levels/world/thumbnail.png`.
    2. Create a 96x64 PNG depicting the Dungeon map similarly. Save as `public/assets/images/levels/dungeon_1/thumbnail.png`.
    3. Both files: PNG, exactly 96x64, no anti-aliasing (preserve pixel-art aesthetic — UI-SPEC §"Asset Surface" line 218).

    Option B — One-off ImageMagick downscale (acceptable placeholder for milestone):
    Run (requires `magick` on PATH):
    ```
    magick "public/assets/images/levels/world/world_background.png" -resize 96x64^ -gravity center -extent 96x64 -filter Point "public/assets/images/levels/world/thumbnail.png"
    magick "public/assets/images/levels/dungeon_1/dungeon_1_background.png" -resize 96x64^ -gravity center -extent 96x64 -filter Point "public/assets/images/levels/dungeon_1/thumbnail.png"
    ```
    Adjust source filenames if dungeon background is named differently — run `ls public/assets/images/levels/dungeon_1/` to confirm. `-filter Point` preserves pixel-art sharpness.

    Verify both files report 96x64:
    ```
    magick identify public/assets/images/levels/world/thumbnail.png
    magick identify public/assets/images/levels/dungeon_1/thumbnail.png
    ```
    Each should print `... PNG 96x64 ...`.

    UI-SPEC mandates a 0x223366 solid-rectangle fallback in code (Plan 03 implements it), so a missing thumbnail at runtime does not crash. For this phase both files SHOULD exist; if you choose "use fallback only" the runtime UI still works, just without thumbnails.
  </how-to-verify>
  <resume-signal>Type "thumbs ready" (after confirming both files exist at 96x64) or "use fallback only" if shipping without thumbnails — Tasks 2-3 still register the keys, and Plan 03's runtime fallback handles missing textures.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Register the two thumbnails in assets.json</name>
  <files>public/assets/data/assets.json</files>
  <read_first>
    - public/assets/data/assets.json (entire file — locate the existing `levels/world` pack around lines 318-334 and the analogous `levels/dungeon_1` pack)
    - .planning/phases/09-lobby-format-map-configuration/09-PATTERNS.md §"src/scenes/preload-scene.ts / public/assets/data/assets.json" (verbatim asset-pack image-entry pattern)
  </read_first>
  <action>
    Edit `public/assets/data/assets.json`:

    1. Inside the existing `{ "path": "assets/images/levels/world", "files": [ ... ] }` pack entry, append a new file object as the last element of the `files` array:
    ```json
    { "type": "image", "key": "MAP_THUMB_WORLD", "url": "thumbnail.png" }
    ```

    2. Inside the existing `{ "path": "assets/images/levels/dungeon_1", "files": [ ... ] }` pack entry, append:
    ```json
    { "type": "image", "key": "MAP_THUMB_DUNGEON_1", "url": "thumbnail.png" }
    ```

    3. Preserve every existing entry and the file's overall JSON structure. Mind comma placement — JSON forbids trailing commas, so the new entry goes BEFORE the closing `]` of the `files` array and needs a comma after the previous-last entry.

    Do NOT add a new pack entry — extend the existing ones so loading happens in the same round-trip as the rest of each level's assets.
  </action>
  <verify>
    <automated>node -e "const a=require('./public/assets/data/assets.json');const flat=JSON.stringify(a);['MAP_THUMB_WORLD','MAP_THUMB_DUNGEON_1'].forEach(k=>{if(!flat.includes(k)){console.error('missing '+k);process.exit(1);}});console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('public/assets/data/assets.json','utf8'))"` exits 0 (file remains valid JSON).
    - `grep -n "MAP_THUMB_WORLD" public/assets/data/assets.json` returns one match.
    - `grep -n "MAP_THUMB_DUNGEON_1" public/assets/data/assets.json` returns one match.
    - `grep -nB2 "MAP_THUMB_WORLD" public/assets/data/assets.json` shows the entry sits inside the `levels/world` pack (the preceding lines reference `assets/images/levels/world`).
    - `grep -nB2 "MAP_THUMB_DUNGEON_1" public/assets/data/assets.json` shows the entry sits inside the `levels/dungeon_1` pack.
    - The smoke test in `<automated>` prints `OK`.
  </acceptance_criteria>
  <done>Two new image entries registered in assets.json under the existing level packs; JSON remains valid.</done>
</task>

<task type="auto">
  <name>Task 3: Add ASSET_KEYS.MAP_THUMB_WORLD and ASSET_KEYS.MAP_THUMB_DUNGEON_1 constants</name>
  <files>src/common/assets.ts</files>
  <read_first>
    - src/common/assets.ts (entire file — confirm the existing ASSET_KEYS export shape; the level constants like `WORLD_BACKGROUND` and `WORLD_FOREGROUND` are the closest analog)
    - .planning/phases/09-lobby-format-map-configuration/09-PATTERNS.md §"Thumbnail-key convention" (mandates SCREAMING_SNAKE — `MAP_THUMB_WORLD` not `map-thumb-world`)
  </read_first>
  <action>
    Edit `src/common/assets.ts`:

    1. Find the `ASSET_KEYS` export (it is either an `as const` object literal or an enum-style export). Match the existing level-key style verbatim.

    2. Add two new keys, grouped with the existing level keys (next to `WORLD_BACKGROUND` / `DUNGEON_1_*` if present). The key name and string value must both be `MAP_THUMB_WORLD` (and `MAP_THUMB_DUNGEON_1`), matching the asset-pack `key` field added in Task 2:
    ```ts
    MAP_THUMB_WORLD: 'MAP_THUMB_WORLD',
    MAP_THUMB_DUNGEON_1: 'MAP_THUMB_DUNGEON_1',
    ```
    Use whatever syntax matches the file's existing style (e.g., `as const` object literal vs. `enum`). Do not introduce a new export pattern.

    3. Do NOT modify any other key. Do NOT reorder existing keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MAP_THUMB_WORLD" src/common/assets.ts` returns one match (key declaration).
    - `grep -n "MAP_THUMB_DUNGEON_1" src/common/assets.ts` returns one match.
    - `grep -nE "MAP_THUMB_WORLD['\"]?\\s*[:=]\\s*['\"]MAP_THUMB_WORLD['\"]" src/common/assets.ts` returns one match (key name and string value identical).
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (client TypeScript compiles).
  </acceptance_criteria>
  <done>ASSET_KEYS exports the two new MAP_THUMB_* constants and the project compiles.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. Both thumbnail PNG files exist at 96x64 (or user explicitly chose fallback-only mode).
2. assets.json is valid JSON and contains both MAP_THUMB_* entries inside the correct existing packs.
3. ASSET_KEYS exports both MAP_THUMB_* constants.
4. `npx tsc --noEmit -p tsconfig.json` exits 0.
5. PreloadScene was NOT modified (it loads the pack via the existing `this.load.pack(ASSET_PACK_KEYS.MAIN, ...)` call — new entries are picked up automatically).
</verification>

<success_criteria>
- Plan 03 can reference `ASSET_KEYS.MAP_THUMB_WORLD` and `ASSET_KEYS.MAP_THUMB_DUNGEON_1` and the textures resolve at runtime.
- Naming reconciled: SCREAMING_SNAKE matches the existing codebase convention (pattern-mapper recommendation #1 honored).
- The `thumbnailKey` strings in `MAP_POOL` (declared in Plan 01) match the asset-pack `key` field and the ASSET_KEYS value exactly — three places, one identifier.
</success_criteria>

<output>
After completion, create `.planning/phases/09-lobby-format-map-configuration/09-02-SUMMARY.md` documenting:
- Whether real thumbnails or downscaled backgrounds were used
- The exact path each PNG lives at and its dimensions
- The two new ASSET_KEYS constants and the assets.json entries
- A confirmation that PreloadScene needed no code change
</output>
