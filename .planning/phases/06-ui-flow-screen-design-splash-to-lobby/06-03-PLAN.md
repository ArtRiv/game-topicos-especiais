---
phase: 06-ui-flow-screen-design-splash-to-lobby
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scenes/menu-placeholder.ts
  - src/scenes/main-menu-scene.ts
  - src/scenes/lobby-scene.ts
  - src/scenes/credits-scene.ts
  - src/scenes/options-scene.ts
  - .planning/phases/06-ui-flow-screen-design-splash-to-lobby/SCREENS.md
autonomous: true
requirements: [UI-04, UI-05, UI-06]
must_haves:
  truths:
    - "Every Phaser Text object in every menu scene uses setResolution(2) — no blurry text anywhere"
    - "SCREENS.md documents every screen: scene key, music, text content, navigation links, and animation"
    - "The navigation flow (Splash → Main Menu → sub-scenes → Lobby) is explicitly mapped in SCREENS.md"
  artifacts:
    - path: ".planning/phases/06-ui-flow-screen-design-splash-to-lobby/SCREENS.md"
      provides: "Screen design documentation"
      contains: "Splash, Main Menu, Create Lobby, Join Lobby, Options, Credits, Lobby"
  key_links:
    - from: "SCREENS.md"
      to: "src/scenes/*.ts"
      via: "Scene key references"
      pattern: "SCENE_KEYS\\."
---

<objective>
(1) Audit and fix font-resolution consistency: every Text object in every menu/UI scene must use `setResolution(2)` to prevent blurry text on upscaled canvases. (2) Produce SCREENS.md — the authoritative screen design document covering every screen from splash to lobby.

Purpose: Fulfills UI-04 (consistent pixel-art font), UI-05 (every screen documented), UI-06 (navigation flow mapped). This plan runs in Wave 1 in parallel with 06-01 — it is a pure audit + doc task with no dependency on the splash scene.

Output: Patched text resolution in lobby-scene.ts + credits-scene.ts + options-scene.ts (which use inline font defs without setResolution), and SCREENS.md.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Font resolution pattern — apply uniformly everywhere -->

Correct pattern (from MainMenuScene, already compliant):
```typescript
const FONT_RESOLUTION = 2;
this.add.text(cx, cy, 'TEXT', { fontFamily: 'FONT_PRESS_START_2P', fontSize: '10px', color: '#fff' })
  .setOrigin(0.5)
  .setResolution(FONT_RESOLUTION);
```

Non-compliant pattern (missing setResolution — found in lobby-scene.ts):
```typescript
const FONT = { fontFamily: '"Press Start 2P"', fontSize: '10px', color: '#ffffff' };
this.add.text(cx, cy, 'TEXT', FONT)  // ← missing .setResolution(2)
```

Note: lobby-scene.ts uses the family name `"Press Start 2P"` (quoted CSS form) while other
scenes use `'FONT_PRESS_START_2P'` (asset-key form). Both are registered via @font-face in
index.html and are functionally identical. Standardize to `'FONT_PRESS_START_2P'` (asset-key
form, no quotes) when adding setResolution — do not bulk-rename the font key in lobby-scene,
just add setResolution to each this.add.text() call there.

Scenes confirmed already compliant (use setResolution(2) via menu-placeholder.ts or directly):
  - MainMenuScene — compliant
  - buildMenuPlaceholder (used by CreateLobby, JoinLobby, Account, Options, Credits) — compliant

Scenes needing audit/fix:
  - LobbyScene — many this.add.text() calls; none have .setResolution(2)
  - CreditsScene (body of create) — uses buildMenuPlaceholder (compliant), nothing extra
  - OptionsScene — same, nothing extra
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Font-resolution audit and fix in LobbyScene</name>
  <files>src/scenes/lobby-scene.ts</files>
  <action>
Read lobby-scene.ts in full. Find every `this.add.text(...)` call. Chain `.setResolution(2)` after the existing chain on each one.

The file defines several font constant objects at the top:
```typescript
const FONT = { fontFamily: '"Press Start 2P"', fontSize: '10px', color: '#ffffff' };
const FONT_TITLE = { fontFamily: '"Press Start 2P"', fontSize: '14px', color: '#ffdd55' };
const FONT_SMALL = { fontFamily: '"Press Start 2P"', fontSize: '8px', color: '#cccccc' };
const FONT_SMALL_WHITE = { fontFamily: '"Press Start 2P"', fontSize: '8px', color: '#ffffff' };
```

Strategy: Rather than editing every call site, add a helper inline:
```typescript
// Add near the top of the file, after the FONT constants:
function t(scene: Phaser.Scene, x: number, y: number, text: string, style: object): Phaser.GameObjects.Text {
  return scene.add.text(x, y, text, style).setResolution(2) as Phaser.GameObjects.Text;
}
```
Then replace `this.add.text(` with `t(this, ` in every occurrence within the file.

If that refactor is too risky (changes argument shape), instead chain `.setResolution(2)` individually on each call. Both approaches are acceptable — choose whichever produces fewer diffs. The result must be that every `this.add.text` in lobby-scene.ts has `.setResolution(2)` in its chain.

Do NOT change any logic, layout values, or networking code — font resolution only.
  </action>
  <verify>
    <automated>cd c:\Users\Arthu\Desktop\code\game-topicos-especiais ; pnpm build 2>&1 | Select-Object -Last 10</automated>
  </verify>
  <done>Build passes. Every this.add.text() in lobby-scene.ts has setResolution(2) chained. No logic changes made.</done>
</task>

<task type="auto">
  <name>Task 2: Create SCREENS.md — screen design & navigation map</name>
  <files>.planning/phases/06-ui-flow-screen-design-splash-to-lobby/SCREENS.md</files>
  <action>
Create the SCREENS.md documentation file. It must include one section per screen, covering:
- Scene key (from SCENE_KEYS)
- File path
- Music playing
- Background / visual treatment
- All text content (exact strings shown in game)
- Navigation links (what scenes it can go to and how)
- Animations (current or planned)
- Design notes / TODOs

Structure it as follows (fill in accurately based on the actual codebase):

```markdown
# Screen Design Map — Phase 06
> Authoritative reference for all screens from Splash to Lobby.
> Last updated: 2026-04-06

## Navigation Flow

Splash → Main Menu → Create Lobby → Lobby (→ Game)
                   ↘ Join Lobby   → Lobby (→ Game)
                   ↘ Account      (placeholder)
                   ↘ Options      (placeholder)
                   ↘ Credits      (placeholder)

## Screens

### 1. Splash Screen
**Scene key:** `SPLASH_SCENE`
**File:** `src/scenes/splash-scene.ts` *(created in Plan 06-01)*
**Music:** `menu_music.ogg` (starts playing immediately; handles browser autoplay unlock)
**Background:** Pure black (`0x000000`)
**Text content:**
  - `"PRESS ANYTHING TO\nGET HIGH IN THE\nFANTASY"` — font: FONT_PRESS_START_2P 8px #e0e0e0, centered
**Animations:** Text blinks (alpha 1 → 0.1 → 1, 600 ms yoyo loop)
**Navigation:** Any key / pointer click → fade-out 400 ms → Main Menu
**Design notes:** Entry point for the game. Music begins here before MainMenuScene.

---

### 2. Main Menu
**Scene key:** `MAIN_MENU_SCENE`
**File:** `src/scenes/main-menu-scene.ts`
**Music:** `menu_music.ogg` (continued from Splash)
**Background:** Looping MP4 video (`assets/images/ui/landscape.mp4`), cover-scaled with LINEAR filter to avoid NEAREST-mode artifacts; semi-transparent black vignette overlay (alpha 0.5)
**Text content:**
  - Title: `"HIGH FANTASY"` — 20px #ffd700, stroke #000000 2px
  - Subtitle: `"- ONLINE EDITION -"` — 8px #8888cc
  - Menu items: `"CRIAR LOBBY"`, `"ENTRAR EM LOBBY"`, `"CONTA"`, `"OPCOES"`, `"CREDITOS"`, `"SAIR"` — 10px #e0e0e0
  - Footer: `"v0.1 DEV"` — 6px #333355, bottom-right
**Animations (Phase 06-02 — cinematic intro):**
  - On scene start: title, subtitle, and menu items drawn at alpha=0
  - At `MUSIC_DROP_MS` (≈ 14 200 ms): title impact flash (scale 1.3 → 1.0, 250 ms Back.Out), subtitle fades in (400 ms, 150 ms delay), menu items stagger in (300 ms, 60 ms between each)
  - Second visit (back from sub-scene): all items immediately visible — cinematic skipped
  - Hover: selected item turns #ffd700; selector box (alpha 0.25) slides to hovered row
**Navigation:**
  - CRIAR LOBBY → CreateLobbyScene (fade 300 ms, Plan 06-02)
  - ENTRAR EM LOBBY → JoinLobbyScene (fade 300 ms)
  - CONTA → AccountScene (fade 300 ms)
  - OPCOES → OptionsScene (fade 300 ms)
  - CREDITOS → CreditsScene (fade 300 ms)
  - SAIR → console.log (placeholder — wire auth logout in future)
**Design notes:** Video background switched to `landscape.mp4` (configurable via `MENU_BG_VIDEO_PATH` constant). `pixelArt:true` would cause nearest-neighbour on downsampled video frames — cleared to LINEAR on vid `play` event.

---

### 3. Create Lobby
**Scene key:** `CREATE_LOBBY_SCENE`
**File:** `src/scenes/create-lobby-scene.ts`
**Music:** `menu_music.ogg` (continued)
**Background:** Dark blue accent bar layout (via `buildMenuPlaceholder`)
**Text content:**
  - Title: `"CRIAR LOBBY"` — 16px #ffdd55
  - Hint: `"Configure sua partida e convide jogadores."`
  - Button: `"CONECTAR AO SERVIDOR"`
  - Back: `"← VOLTAR"`
**Animations:** Fade-in on entry (300 ms, Plan 06-02)
**Navigation:**
  - CONECTAR AO SERVIDOR → LobbyScene (startScene fade 300 ms)
  - VOLTAR → Main Menu (fade 300 ms)
**Design notes:** Placeholder. To be evolved with real lobby-creation form (room code, game mode picker, player list) once networking protocol supports it.

---

### 4. Join Lobby
**Scene key:** `JOIN_LOBBY_SCENE`
**File:** `src/scenes/join-lobby-scene.ts`
**Music:** `menu_music.ogg` (continued)
**Background:** Same as Create Lobby (buildMenuPlaceholder)
**Text content:**
  - Title: `"ENTRAR EM LOBBY"` — 16px #ffdd55
  - Hint: `"Encontre uma partida em andamento."`
  - Button: `"CONECTAR AO SERVIDOR"`
  - Back: `"← VOLTAR"`
**Animations:** Fade-in on entry (300 ms)
**Navigation:**
  - CONECTAR AO SERVIDOR → LobbyScene (fade)
  - VOLTAR → Main Menu (fade)
**Design notes:** Placeholder. To be evolved with lobby browser / code-entry input.

---

### 5. Account
**Scene key:** `ACCOUNT_SCENE`
**File:** `src/scenes/account-scene.ts`
**Music:** `menu_music.ogg` (continued)
**Background:** buildMenuPlaceholder layout
**Text content:**
  - Title: `"CONTA"` — 16px #ffdd55
  - Hint: `"Login, perfil e conquistas. (Em breve)"`
  - Back: `"← VOLTAR"`
**Animations:** Fade-in on entry (300 ms)
**Navigation:** VOLTAR → Main Menu (fade)
**Design notes:** Placeholder — connect to Google OAuth auth flow in Phase 1 (v2.0 milestone).

---

### 6. Options
**Scene key:** `OPTIONS_SCENE`
**File:** `src/scenes/options-scene.ts`
**Music:** `menu_music.ogg` (continued)
**Background:** buildMenuPlaceholder layout
**Text content:**
  - Title: `"OPCOES"` — 16px #ffdd55
  - Hint: `"Audio, controles e video. (Em breve)"`
  - Back: `"← VOLTAR"`
**Animations:** Fade-in on entry (300 ms)
**Navigation:** VOLTAR → Main Menu (fade)
**Design notes:** Placeholder. Planned: master volume slider (MusicManager.setVolume), key remapping, display settings.

---

### 7. Credits
**Scene key:** `CREDITS_SCENE`
**File:** `src/scenes/credits-scene.ts`
**Music:** `menu_music.ogg` (continued)
**Background:** buildMenuPlaceholder layout
**Text content:**
  - Title: `"CREDITOS"` — 16px #ffdd55
  - Hint: `"A equipe por tras do jogo. (Em breve)"`
  - Back: `"← VOLTAR"`
**Animations:** Fade-in on entry (300 ms)
**Navigation:** VOLTAR → Main Menu (fade)
**Design notes:** Placeholder. Planned: tween-scrolling credits text (team names, asset licenses, tool credits).

---

### 8. Lobby (Networking)
**Scene key:** `LOBBY_SCENE`
**File:** `src/scenes/lobby-scene.ts`
**Music:** `menu_music.ogg` (continued — MusicManager no-op if already playing)
**Background:** Solid dark (#080818 equivalent via graphics, not a rect)
**Text content (Connect view):**
  - Title: `"MAGES ONLINE"` — 14px #ffdd55
  - Labels: `"SERVER IP:"`, `"NICKNAME:"`
  - DOM inputs: IP text field (default: "localhost"), nickname (max 12 chars)
  - Button: `"CONNECT"`
**Text content (Lobby List view):** Lobby cards with name, player count; CREATE / JOIN buttons
**Text content (Lobby Room view):** Player list, team assignment UI, READY / START buttons
**Music cues:** None — stays on menu music until GameScene starts
**Navigation:**
  - CONNECT → connects to server → shows Lobby List view (internal state machine, no scene switch)
  - Lobby start → GameScene (via NETWORK_LOBBY_STARTED event)
**Design notes:** Full networking UI — not a placeholder. Font setResolution(2) audit applied in Plan 06-03. Uses DOM elements (this.add.dom) for IP/nick inputs — requires `dom.createContainer: true` in Phaser config (already set in main.ts).

---

## Music Cue Map

| Screen | Track | Notes |
|--------|-------|-------|
| Splash | menu_music.ogg | Loaded in preload(), played on first user interaction |
| Main Menu | menu_music.ogg | Continues from splash; cinematic timed to drop at ≈14 200 ms |
| Create / Join / Options / Credits / Account | menu_music.ogg | Continuous — MusicManager no-ops on repeat calls |
| Lobby | menu_music.ogg | Continuous through lobby wait |
| Game | gameplay_music.ogg | Switches on GameScene.create() |

## Font Consistency

All UI text uses `FONT_PRESS_START_2P` with `.setResolution(2)` applied. Both font-family aliases (`'Press Start 2P'` and `'FONT_PRESS_START_2P'`) are registered in index.html via @font-face from the same TTF file (`/assets/fonts/Press_Start_2P/PressStart2P-Regular.ttf`).

Future upgrade: export a .fnt + .png bitmap font from the TTF and switch all Text to BitmapText for pixel-perfect rendering without the canvas 2x resolution workaround.

## Transition Specification

All transitions use `startScene(scene, key, 300)` from `src/scenes/scene-transition.ts` (Plan 06-02):
- Duration: 300 ms fade-out (black)
- Entry: 300 ms fade-in (black) via `cameras.main.fadeIn(300, 0, 0, 0)` in buildMenuPlaceholder and SplashScene
- Exception: Splash → Main Menu uses 400 ms fade-out for dramatic effect
```

Write this content to `.planning/phases/06-ui-flow-screen-design-splash-to-lobby/SCREENS.md`.
  </action>
  <verify>
    <automated>Test-Path ".planning/phases/06-ui-flow-screen-design-splash-to-lobby/SCREENS.md"</automated>
  </verify>
  <done>SCREENS.md exists. Covers all 8 screens. Navigation flow diagram present. Music cue map and font consistency note included.</done>
</task>

</tasks>

<verification>
`pnpm build` exits 0. SCREENS.md exists and covers every screen listed in the roadmap (Splash, Main Menu, Create Lobby, Join Lobby, Account, Options, Credits, Lobby). Every `this.add.text()` in lobby-scene.ts has `.setResolution(2)`.
</verification>

<success_criteria>
- All this.add.text() calls in lobby-scene.ts have .setResolution(2)
- SCREENS.md exists at .planning/phases/06-ui-flow-screen-design-splash-to-lobby/SCREENS.md
- SCREENS.md covers all screens with: scene key, file, music, text content, animations, navigation
- Navigation flow diagram present in SCREENS.md
- TypeScript build passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-flow-screen-design-splash-to-lobby/06-03-SUMMARY.md`
</output>
