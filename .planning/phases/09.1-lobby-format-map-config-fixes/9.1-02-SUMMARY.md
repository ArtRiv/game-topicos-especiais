---
phase: 09.1-lobby-format-map-config-fixes
plan: 02
subsystem: lobby/ui
tags: [layout, ui, lobby, gap-closure]
provides:
  - lobby-layout: player-list-y-clearance
  - lobby-layout: format-row-centerline-alignment
requires:
  - phase-09 lobby render path (#renderConfigBlock, #renderPlayerList)
affects:
  - src/scenes/lobby-scene.ts
tech-stack:
  added: []
  patterns:
    - "Vertical centering via shared y + origin (0, 0.5) for mixed Phaser.Text + DOMElement rows"
key-files:
  created: []
  modified:
    - src/scenes/lobby-scene.ts
decisions:
  - "Use 248 (= 232 label-bottom + 16 md token) for player-list baseY — multiple of 4, aligns with UI-SPEC spacing scale"
  - "Align Format label + <select> by sharing y=127 with origin (0, 0.5) — robust to intrinsic height differences"
metrics:
  duration: ~5min
  completed: 2026-05-21
  tasks: 2
  files_changed: 1
---

# Phase 09.1 Plan 02: Lobby Layout Polish Summary

One-liner: Player list now renders below map card labels with 16px clearance, and the Format label/<select> share a vertical centerline at y=127.

## Tasks Completed

| Task | Description | Commit |
| ---- | ----------- | ------ |
| 1    | Push #renderPlayerList baseY from 220 → 248 | `6ea43b0` |
| 2    | Align Format label and DOM select at y=127 with origin (0, 0.5) | `7d760a5` |

## Final Y-Anchor Table

| Element | Before | After | Origin (after) |
|---|---|---|---|
| Capacity header | 96 | 96 (unchanged) | (0.5, 0) |
| Format label (host) | 116 | **127** | **(0, 0.5)** |
| DOM `<select>` (host) | 116 | **127** | **(0, 0.5)** |
| Map: label (host) | 140 | 140 (unchanged) | (0, 0) |
| Map card top | 148 | 148 (unchanged) | — |
| Map card label | 220 | 220 (unchanged) | (0.5, 0) |
| Player list row 0 | 220 | **248** | default |
| #statusText | cy + 80 | cy + 80 (unchanged) | — |
| START GAME btn | cy + 120 | cy + 120 (unchanged) | — |

Clearance verification: map-card label visual bottom (~y=232) + 16px md = y=248 ≥ player-list baseY. Gap closed.

## Gaps Closed

- **UAT Test 1 sub-gap 1b (major):** Player list rows now have 16px clearance below map card labels — no visual overlap.
- **UAT Test 4 out-of-band (cosmetic):** Format label and `<select>` share centerline y=127 — no perceptible baseline mismatch.

## Verification

- `npx vite build` — passed (5.42s, 1664 kB bundle).
- TS check: pre-existing node_modules type errors (rollup/parseAst, lib.dom TextDecoder) are unrelated to this plan — no new errors introduced in `src/scenes/lobby-scene.ts`.
- Acceptance criteria checks: `const baseY = 248;` present (1 match), old `220` gone, label/select both at y=127 with `setOrigin(0, 0.5)`.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- FOUND: src/scenes/lobby-scene.ts (baseY=248 at line 339; Format label y=127 line 248; formatDom y=127 line 251)
- FOUND: commit 6ea43b0
- FOUND: commit 7d760a5
