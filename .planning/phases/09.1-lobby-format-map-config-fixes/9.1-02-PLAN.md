---
phase: 09.1-lobby-format-map-config-fixes
parent_phase: 09-lobby-format-map-configuration
plan: 02
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/scenes/lobby-scene.ts
autonomous: true
requirements: []
gaps_addressed:
  - uat_test: 1
    sub_gap: 1b
    truth: "Host's player list does not visually overlap the map-control area"
    severity: major
  - uat_test: 4
    truth: "'Format:' label and the format <select> input render on the same vertical baseline"
    severity: cosmetic

must_haves:
  truths:
    - "Host's player list rows render with at least 16px clear vertical space between the bottom of the map preview cards (and their displayName labels) and the top of the first player row"
    - "The 'Format:' text label and the DOM <select> input are vertically centered against each other — no perceptible baseline mismatch"
    - "Non-host view also gets the same player-list Y push (so non-host rows do not overlap the non-host 'Map: <name>' label at y=140)"
    - "All other layout anchors stay within the 720px-tall camera viewport budget (per UI-SPEC line 184)"
  artifacts:
    - path: "src/scenes/lobby-scene.ts"
      provides: "Updated #renderPlayerList baseY constant; updated Format label y/origin in #renderConfigBlock to match <select> centerline; map-card displayName label y nudge if needed"
      contains: "#renderPlayerList"
  key_links:
    - from: "#renderConfigBlock map cards"
      to: "#renderPlayerList baseY"
      via: "y-anchor math — cards bottom edge + label gutter + 16px gap = player list baseY"
      pattern: "baseY"
    - from: "Format text label"
      to: "DOM <select> element"
      via: "shared y coordinate with matching vertical origin (both center-aligned around the same baseline)"
      pattern: "'Format:'"
  evidence:
    - "UAT gap entry: .planning/phases/09-lobby-format-map-configuration/09-UAT.md#gaps (1st entry, sub-gap 1b — 'Player list base Y (220) does not account for the actual rendered height of the map control block')"
    - "UAT gap entry: 2nd entry — Format label vs <select> baseline mismatch"
    - "Plan 09-03 SUMMARY layout table (lines 47-62): cards at y=180 with height 64, label at y=cardY+40=220 -> label TOP at y=220 -> overlaps player list whose baseY is ALSO 220"
    - "src/scenes/lobby-scene.ts:277 — `cardLabel` at `cardY + 40` (y=220) and src/scenes/lobby-scene.ts:339 — player list `baseY = 220` — these collide exactly"
    - "src/scenes/lobby-scene.ts:248 — Format label `setOrigin(0, 0)` at y=116; src/scenes/lobby-scene.ts:251-253 — DOM <select> at y=116 also `setOrigin(0, 0)` but the <select> has ~22px intrinsic height so its visual center is at y=127 while the label's visual center is at y=120"
---

<objective>
Two related layout fixes in `src/scenes/lobby-scene.ts`, both in the waiting-room render path:

1. **Player list Y push (gap 1b — major):** The map-card displayName label sits at `cardY + 40 = 220`, and the player list `baseY` is also 220. The two overlap pixel-for-pixel. Push the player list baseY down to clear the card-label bottom edge with at least 16px (one `md` token from UI-SPEC) of breathing room.
2. **Format label↔select alignment (cosmetic):** The 'Format:' text and the DOM `<select>` use the same y=116 anchor but different visual heights, so the label appears to "float" above the select's center. Re-align their visual centerlines.

Single file, single render pass, atomic visual change — bundled into one plan to keep the cosmetic and layout fixes in lockstep (they are interdependent: nudging the format row also affects the budget below it).

Purpose: Closes UAT gaps 1b (player list overlap — major) and the out-of-band Format alignment gap (cosmetic). Restores the layout contract described in UI-SPEC §"Layout Anchors".

Output: A cleaner waiting-room render where player-list rows breathe below the map cards, and the Format row reads as a single visually-aligned control.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09.1-lobby-format-map-config-fixes/09.1-CONTEXT.md
@.planning/phases/09-lobby-format-map-configuration/09-UAT.md
@.planning/phases/09-lobby-format-map-configuration/09-UI-SPEC.md
@.planning/phases/09-lobby-format-map-configuration/09-03-SUMMARY.md
@src/scenes/lobby-scene.ts

<interfaces>
<!-- Current layout (from src/scenes/lobby-scene.ts + 09-03-SUMMARY layout table) -->

| Element | Current Y | Computed bottom edge | Notes |
|---|---|---|---|
| WAITING ROOM title | 40 | ~54 | FONT_TITLE 14px |
| Host: <name> | 70 | ~80 | FONT_SMALL 8px |
| Capacity header | 96 | ~108 | FONT 10px, origin (0.5, 0) |
| Format label (host) | 116 | ~124 | FONT_SMALL_WHITE 8px, origin (0, 0) at y=116 |
| DOM <select> (host) | 116 | ~138 | origin (0, 0) at y=116, intrinsic height ~22px |
| Map: label (host) | 140 | ~148 | FONT_SMALL_WHITE 8px |
| Map card top edge | 180 - 32 = 148 | rect center y=180, h=64 -> top=148, bottom=212 |  |
| Map card displayName | 180 + 40 = 220 | ~228 | text origin (0.5, 0) at y=220 |
| Player list row 0 | 220 | 220 + 36 = 256 | text origin default (0, 0) at y=220 |
| #statusText | cy + 80 = 240 | ~248 | (camera centerY ≈ 160 for 320px-tall scale, so this lands BELOW the first few player rows; pre-existing concern, NOT part of this gap) |
| START GAME btn | cy + 120 = 280 | ~294 | (host only) |

Camera viewport: declared 320px tall in main.ts (`height: 320`), NOT 720 as UI-SPEC line 184 assumed. The HEIGHT_CONTROLS_WIDTH scale mode keeps it at 320 logical px. That means the original UI-SPEC layout budget WAS over-generous; we still need to fit everything in 320px logical.

10-row max player list at 36px pitch = 360px — already overflows the 320px viewport, which is a known issue with the 10v10 cap but NOT this plan's scope. We only fix the cards/list overlap at the TOP of the list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push player list baseY to clear map card displayName bottom edge</name>
  <files>src/scenes/lobby-scene.ts</files>
  <read_first>
    - src/scenes/lobby-scene.ts:226-291 (#renderConfigBlock — note the cardY = 180 and the cardLabel at cardY + 40)
    - src/scenes/lobby-scene.ts:334-388 (#renderPlayerList — baseY = 220 at line 339)
    - .planning/phases/09-lobby-format-map-configuration/09-UI-SPEC.md spacing section (md = 16px, lg = 24px tokens)
  </read_first>
  <action>
    Edit `src/scenes/lobby-scene.ts` `#renderPlayerList` method (~line 334):

    Change `const baseY = 220;` to `const baseY = 248;`.

    Rationale: map card displayName label is `add.text(cardX, cardY + 40, ...)` with `cardY = 180`, so the label's top edge is at y=220. The label is FONT_SMALL_WHITE 8px text — actual rendered height is ~12px (with default line height). The label's visual bottom edge therefore sits at ~y=232. Adding the `md=16px` spacing token from UI-SPEC yields a player-list `baseY` of **248**.

    Why 248 (not 250 or 256):
    - 232 (label bottom) + 16 (md token) = 248
    - Stays a multiple of 4 (alignment with the spacing scale in UI-SPEC §"Spacing Scale")
    - Keeps the rest of the layout unchanged — the START GAME button at `cy + 120` and #statusText at `cy + 80` are anchored to camera-relative coordinates, not the player list, so they are unaffected

    Do NOT touch any other y constant in this method or in #renderConfigBlock. Do NOT change the card layout (cards stay at y=180; their labels stay at cardY+40).

    Do NOT introduce a constant export — keep `baseY` as a local `const` inside `#renderPlayerList` (matches the existing style of the file; no other layout constants are top-level).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; node -e "const f=require('fs').readFileSync('src/scenes/lobby-scene.ts','utf8');const m=f.match(/const baseY = (\d+);/g)||[];if(!m.some(s=>s.includes('248'))){console.error('player list baseY not pushed to 248');process.exit(1);}console.log('baseY OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "const baseY = 248;" src/scenes/lobby-scene.ts` returns exactly 1 match (inside #renderPlayerList).
    - `grep -nE "const baseY = 220;" src/scenes/lobby-scene.ts` returns 0 matches (the old value is gone; note that another `const baseY = 155;` in #renderLobbyList may still be present — that one is intentional and out of scope).
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - Manual smoke: host creates lobby with 1-3 players. The first player row's colored dot/name renders clearly BELOW the map card displayName labels, with visible empty space between them (no overlap).
  </acceptance_criteria>
  <done>`#renderPlayerList` uses `baseY = 248`; player rows render below the map cards with 16px clearance.</done>
</task>

<task type="auto">
  <name>Task 2: Align Format label and DOM <select> centerlines</name>
  <files>src/scenes/lobby-scene.ts</files>
  <read_first>
    - src/scenes/lobby-scene.ts:246-260 (#renderConfigBlock host branch — Format label at line 248, formatDom at lines 251-253)
    - .planning/phases/09-lobby-format-map-configuration/09-UI-SPEC.md §"Layout Anchors" (format row anchor y=116)
  </read_first>
  <action>
    Edit `src/scenes/lobby-scene.ts` `#renderConfigBlock` method, host branch. Two coordinated changes (DO BOTH, atomically — they are visually interdependent):

    1. **Format label origin and y.** Replace:
       ```ts
       const formatLabel = this.add.text(cx - 150, 116, 'Format:', FONT_SMALL_WHITE).setOrigin(0, 0);
       ```
       with:
       ```ts
       const formatLabel = this.add.text(cx - 150, 127, 'Format:', FONT_SMALL_WHITE).setOrigin(0, 0.5);
       ```

       Rationale: change vertical origin to 0.5 (center) and anchor at y=127. The DOM `<select>` lives in a Phaser DOMElement whose default origin is (0.5, 0.5), so a select at y=116 with intrinsic height ~22px has its visual center at y=116. The label here matches that centerline.

       Wait — re-check: the existing formatDom call is `.setOrigin(0, 0)` at line 253 ("setOrigin(0, 0)"). That puts the select's TOP at y=116, so center is at y=116 + 11 = 127. We align the label's center to y=127.

    2. **Adjust the formatDom y AND origin** to make the centerlines explicit. Replace the existing:
       ```ts
       const formatDom = this.add.dom(cx + 30, 116).createFromHTML(
         `<select style="...">${optionsHtml}</select>`,
       ).setOrigin(0, 0);
       ```
       with:
       ```ts
       const formatDom = this.add.dom(cx + 30, 127).createFromHTML(
         `<select style="...">${optionsHtml}</select>`,
       ).setOrigin(0, 0.5);
       ```
       (Same options HTML — do not retype it; just change the y from `116` to `127` and the `.setOrigin(0, 0)` to `.setOrigin(0, 0.5)`. Both elements now share y=127 with vertical origin 0.5, so their visual centerlines coincide regardless of intrinsic-height differences.)

    Constraints:
    - Do NOT change the x coordinates (label stays at `cx - 150`, select stays at `cx + 30`).
    - Do NOT change the select's HTML / options / change-event wiring.
    - Do NOT touch the `Map:` label (line 263) or any other element in this method.
    - The capacity header at y=96 with `setOrigin(0.5, 0)` is also unchanged.

    Note: this is a 4px vertical nudge (116 -> 127 ≈ +11 with origin change) of both elements together. The Map: label at y=140 still sits 13px below the bottom of the format row, well clear of overlap.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; node -e "const f=require('fs').readFileSync('src/scenes/lobby-scene.ts','utf8');const labelMatch=/this\.add\.text\(cx - 150, 127, 'Format:', FONT_SMALL_WHITE\)\.setOrigin\(0, 0\.5\)/.test(f);const domMatch=/this\.add\.dom\(cx \+ 30, 127\)[\s\S]{0,500}?\.setOrigin\(0, 0\.5\)/.test(f);if(!labelMatch){console.error('Format label not realigned');process.exit(1);}if(!domMatch){console.error('formatDom not realigned');process.exit(1);}console.log('align OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "this\\.add\\.text\\(cx - 150, 127, 'Format:'" src/scenes/lobby-scene.ts` returns exactly 1 match.
    - `grep -nE "this\\.add\\.dom\\(cx \\+ 30, 127\\)" src/scenes/lobby-scene.ts` returns exactly 1 match.
    - `grep -nF ".setOrigin(0, 0.5)" src/scenes/lobby-scene.ts` returns at least 2 matches (label + select; could be more if other elements use it elsewhere — that's fine).
    - `grep -nE "this\\.add\\.text\\(cx - 150, 116" src/scenes/lobby-scene.ts` returns 0 matches (old anchor gone).
    - `grep -nE "this\\.add\\.dom\\(cx \\+ 30, 116\\)" src/scenes/lobby-scene.ts` returns 0 matches (old anchor gone).
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `npx vite build` exits 0.
    - Manual smoke: host creates lobby. The text "Format:" and the dropdown render with their visual centers aligned — no perceptible up/down offset between the baseline of "Format:" and the middle of the `<select>` control.
  </acceptance_criteria>
  <done>Format label and DOM select both anchored at y=127 with vertical origin 0.5; visual centerlines coincide.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit -p tsconfig.json` exits 0.
2. `npx vite build` exits 0.
3. Manual smoke (host POV, 2-window UAT replay of Test 1b + Test 4 alignment):
   - Test 1b: player list rows do NOT visually intersect the map card area or its displayName labels.
   - Test 4 alignment: the "Format:" text and the dropdown control read as a single horizontally-arranged row with consistent vertical centering.
4. No regression to capacity header (y=96), Map: label (y=140), map cards (y=180), Map card displayName (cardY+40=220), #statusText (cy+80), or START GAME button (cy+120).
</verification>

<success_criteria>
- UAT gap 1b (player list overlaps map text) closes — Test 1 sub-issue 1b transitions from `issue` to `pass`.
- UAT alignment gap (Format label vs <select>) closes — the out-of-band cosmetic issue is resolved.
- No new layout regressions; the bottom-half of the waiting room (statusText, START button) is byte-identical in render output.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-lobby-format-map-config-fixes/9.1-02-SUMMARY.md` documenting:
- Final y-anchor table (Task 1 changed `baseY` 220 → 248; Task 2 changed Format row 116 → 127 with origin 0.5)
- Manual smoke result for both gaps (screenshots or text description showing the corrected layout)
- Confirmation that no other y anchor was touched (capacity header, Map: label, map cards, statusText, START button all unchanged)
</output>
